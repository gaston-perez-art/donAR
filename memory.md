# memory.md — Proyecto DonAR

Archivo vivo. Se lee al inicio de cada sesión y se actualiza al cerrar. Registra estado, decisiones tomadas, supuestos abiertos y próximos pasos. Complementa (no reemplaza) el "Paper fundacional" y el "Contexto del proyecto y como trabajo".

Última actualización: 31 de julio de 2026 (sesión 21, con Sonnet, ejecutando `docs/PLAN-SONNET-2.md`). **Bloques B, C, D y F del plan, hechos y commiteados** (`29e3b22`, `64d8e69`, `74a6724`, `2ffd2b2`): estados vacíos unificados, skeletons de carga, **Tour 1** (`welcome-tour-screen.tsx`, con placeholder de imagen en vez de esperar las capturas reales, decisión explícita de Gastón que salteó el orden del plan) y **"Cómo funciona"** (`como-funciona.tsx` en el Perfil, fila "Ver la presentación" que relanza el Tour 1 como modal vía `replayTour` en el store, sin tocar el flag de AsyncStorage). Verificación en cada bloque: `npx expo lint` (mismos 2 warnings preexistentes) y `npx expo export --platform ios` **y** `--platform android` limpios. `npx tsc --noEmit` limpio en B/C/D; **en F queda con UN error esperado y no de código:** `router.push('/como-funciona')` no tipa porque `.expo/types/router.d.ts` (generado, gitignored, no se toca a mano) no se regeneró desde el 30 jul, solo lo hace `npx expo start` corriendo de verdad; se resuelve solo la primera vez que se levante el dev server, no bloquea nada real. **Sigue:** Bloque E (Tour 2, coach marks sobre la app real, "el más frágil, hacelo último" según el plan). Lo anterior (sesión 19, la más cargada en frentes distintos: **go-to-market + seguridad + legal + bugs reales + marca**, con Fable 5). En orden: (1) **`docs/go-to-market.md` nuevo**, plan de marketing + landing + financiación con elevator pitch y deck (Épica 14). (2) **Barrido de seguridad**: 4 agujeros de RLS reales encontrados y con SQL de fix, **corridos por Gastón en Supabase el mismo día** (Épica 15.1-15.4); falta un solo paso, confirmar en caliente que los 2 exploits críticos ahora fallan. (3) **Tres documentos nuevos**: `docs/ciberseguridad.md` (registro vivo de seguridad), `docs/privacidad.md` y `docs/terminos-y-condiciones.md` (borradores legales, ninguno revisado por abogado todavía). (4) **Alerta de marca sin resolver**: el dominio que Gastón creía tener (`donar-online.com`) choca de nombre con un competidor real que ya opera (`donaronline.org`); decisión en pausa, él la va a pensar. (5) **Bug real arreglado**: el donante nunca aparecía con su nombre/foto real en Aportes recibidos/Actividad/Lo que recibiste (mostraba "Alguien de la comunidad" hardcodeado); ahora sí, respetando donaciones anónimas. (6) **Combinación de marca implementada** (Épica 13.4/13.5): símbolo real del logo en el loading inicial y en los 3 estados vacíos que reusaban el mismo corazón genérico; el corazón hardcodeado de cada causa reemplazado por color+inicial del título en los 6 lugares que se repetían fila por fila. (7) **Montos grandes**: formato final `"$111 Mill"` (no "$111M"), umbral $10.000.000, aplicado a todo total agregado (perfil, feed, detalle de causa, mini-reporte de cierre, "Mis causas"), nunca a un aporte individual ni a una instrucción de pago. (8) **Nueva Épica 1.6, importante y SIN diseñar:** poder editar una causa (hoy no existe ninguna forma). (9) **Informe del fundador** (artifact, https://claude.ai/code/artifact/c29f0199-c6df-40cc-98bd-f382be5d75bf) actualizado con data fresca de git: 68 commits (antes 23), 10 días de calendario/8 activos (antes 6/5), 47% después de medianoche (antes 61%, la franja 16-24hs ahora pesa 44%), pico de 29 commits en un solo día (29 jul, 43% de todo el proyecto). Scorecard: Seniority en producto subió de 8 a 8,5 (evidencia concreta: pidió la auditoría de seguridad por iniciativa propia, y cuestionó con criterio la sugerencia de usar el logo de marca como ícono de causa en vez de aceptarla de una). Los 3 riesgos del veredicto (OK legal, usuarios reales, paciencia con la plata) siguen exactamente igual que en la 1ª edición: nada de eso se resolvió todavía, es la métrica que más importa y sigue en cero. Todo el código de este cierre está commiteado y pusheado a `main` (`2e20613`); `npx tsc --noEmit` y `npx expo lint` limpios en cada paso. Lo anterior (sesión 18, la más larga hasta ahora: Épicas 1.5, 3, 4, 5, 6 y 10 cerradas o muy avanzadas, más Épica 12 nueva en el backlog). **MVP funcionalmente completo salvo dos cosas bloqueadas por configuración externa** (dominio Resend + redirect URL de Supabase para recuperar contraseña; consulta legal). Épicas 1-3 hechas (eje "conectar sin custodiar" completo, incluye auto-confirmación a 48hs). **Épica 4 completa**: cierre de causa (por monto o por tiempo), mini-reporte, agradecimiento del beneficiado. **Épica 5**: bug real encontrado y arreglado (el ranking sumaba puntos de aportes `pending`/`rejected`, no solo `approved`); sumado mini-perfil de donante "estilo Airbnb" con foto, nivel, medallas y monto donado. **Épica 6**: monto mínimo $5.000. **Épica 10**: nombre y apellido reales (campos separados, iniciales tipo "GP"), foto de perfil, recuperar contraseña (código listo, falta config). **Cierre de la sesión:** tu propia causa ya no aparece en el feed general (regla Instagram), fix de un bug real ("Perfil se recarga sola"), y **Épica 12 nueva** anotada (el curador no puede dar de baja una causa ya publicada, agujero de moderación real). Sesión trabajada con Opus (decisiones de producto) + Sonnet (implementación), según plan de `docs/PLAN-SONNET.md`. MVP de pago = transferencia + aporte voluntario (MP apagado con flag, oculto del todo). Legal documentado. **Ícono/logo real de la app TERMINADO** (mano + corazón sobre squircle celeste), proceso AI-first completo en `docs/proceso-logo.md`. Backlog priorizado en docs/BACKLOG.md, con el detalle línea por línea de todo lo de hoy.

---

## 1. Qué es el proyecto (resumen de una línea)

DonAR: app móvil (Android/iOS) + landing web de colectas persona a persona, con verificación de la causa antes de publicarla, trazabilidad visible del dinero, y gamificación por reconocimiento. Foco inicial Argentina, idioma español.

---

## 2. Cómo trabajar con Gastón (recordatorio permanente)

- Al hueso: directo, sin relleno ni preámbulo.
- **Actualizar `memory.md` de forma RECURRENTE, no solo al cerrar** (pedido explícito de Gastón, 27 jul). Mantenerlo vivo durante la sesión, no dejar que quede viejo. Igual con la doc de negocio (`docs/paper-fundacional-donar.md`, `docs/BACKLOG.md`) cuando surge algo clave.
- Cuando Gastón entra en "explosión de creatividad" y tira muchas ideas desordenadas, el rol es ANCLAR: no codear al toque, ordenar en backlog/mapa, marcar tensiones y dependencias, y recién después priorizar. Él lo pidió textual: "mantené el orden, el eje y el expertise en producto world class, no te me pierdas".
- Nunca inventar: toda afirmación con sustento (fuente, archivo, doc, razonamiento). Los supuestos se marcan como supuestos.
- Trato profesional y medido (terapeuta con paciente).
- Preguntar antes de asumir si la decisión cambia el resultado; si es detalle menor con default obvio, tomarlo y avisar en una línea.
- Decisiones técnicas siempre con justificación y trade-offs (qué se resigna).
- Prohibido el guion largo (—) en cualquier texto.
- Se arranca por el problema y el usuario, no por la tecnología. Validar antes de invertir.
- Antes de codear: ficha del proyecto cerrada + stack propuesto con OK de Gastón.
- Cuando una tarea tiene varias piezas grandes (schema + storage + UI + pantalla nueva), armar y mostrar una lista de tareas para no perder el hilo, e ir marcando el avance real.
- **Paridad iOS/Android SIEMPRE (pedido enfático, 28 jul, en mayúsculas: "esto tiene que ser optimo tanto para iphone como para android. JAMAS NO CONSIDERES ALGUNO").** Todo fix o feature tiene que funcionar bien en las dos plataformas, nunca priorizar una a costa de la otra. Antes de diagnosticar un bug de gestos/layout/UI, preguntar en qué dispositivo se vio (no asumir); y el fix elegido tiene que resolverlo en ambas, no solo en la reportada. Desconfiar de soluciones platform-specific (`Platform.OS`, props solo-Android como `nestedScrollEnabled`, etc.) como fix único.

---

## 3. Estado general del proyecto

- **Etapa actual:** POC móvil con flujo de donación y curaduría real de punta a punta funcionando (feed → detalle → donar, y crear causa → evidencia → revisión de un curador → publicada). Falta integración real de pago (Mercado Pago) y verificación automática de identidad; ambas conscientemente fuera del MVP.
- **Repo:** carpeta local `~/Documents/donAR`, git en rama `main`, remoto `origin` = https://github.com/gaston-perez-art/donAR.git. Commitear directo a `main` es intencional para el POC (confirmado por Gastón, no hace falta rama+PR).
- **Documentos base:** Paper fundacional v0.2, manual de trabajo, y `Flujo de verificacion de causas.md` (v0.1). **`docs/go-to-market.md`** (30 jul, v0.1): capítulo de marketing, landing y financiación (elevator pitch + deck de 12 slides + objeciones), complementa al paper. Copias en `docs/` (CONTEXTO.md, paper-fundacional-donar.md, prototipo-donar.html). **`docs/BACKLOG.md`** (27 jul): backlog vivo del producto por épicas (qué falta y en qué orden). **`docs/proceso-logo.md`** (29 jul): registro AI-first COMPLETO de cómo se generó el ícono/logo de la app, iteración por iteración, con los prompts exactos de ChatGPT de cada paso, el sistema de identidad (símbolo vs. wordmark) y la división de trabajo IA-de-imagen/`sharp`. Acompañan: `scripts/generate-icons.mjs` (genera los 8 assets desde la imagen fuente) y `docs/logo-preview.html` (registro visual autocontenido). El paper ahora incluye la sección **11.b "Confianza del dinero sin custodia"** (modelo antifraude del pago, ver sección 4 de este memory).
- **Ficha del proyecto:** definida. Stack decidido (ver sección 4).

### Avance de rebanadas (código real, no solo README)
- [x] Base Expo corriendo + navegación por tabs con botón + centrado.
- [x] Feed de causas verificadas, con datos reales de Supabase (`causes_public`).
- [x] Detalle de causa + donación con registro en Supabase (pantallas `cause/[id]`, `donate/[id]`), con trazabilidad visible (lista de aportes).
- [x] Crear causa e2e con store local (`create.tsx`, `src/store/causes-store.tsx`).
- [x] **Curaduría real** (26 jul): la causa nace en estado `review`, no se auto-aprueba más. Se pide evidencia (DNI frente/dorso, selfie, documento de respaldo) subida a un bucket privado de Storage. Un curador (por ahora Gastón, marcado a mano con `profiles.is_curator`) la revisa en `/curar` y aprueba, rechaza o pide info. El creador ve el estado de su causa en el feed ("Tus causas") y en el detalle, con motivo si corresponde y botón para reenviar tras un "pedido de info".
- [x] Perfil único (donado + recibido), medallas y nivel con datos reales de Supabase.
- [x] **Identidad cross-device** (26 jul): vincular la sesión anónima a un mail real (código de 6 dígitos, no magic link, para no depender de deep linking en Expo Go). Permite recuperar el historial desde otro dispositivo. Requirió SMTP propio (Resend) porque Supabase no deja editar las plantillas de mail sin uno.
- [x] **Fotos de portada opcionales** (hasta 2, columna `image_urls text[]`, bucket público `cause-covers`): carrusel horizontal con puntitos en `cause-card` y `cause/[id]`; fallback al emoji + color si no hay ninguna. Reemplazó la `image_url` (una sola) del 25 jul.
- [x] **Pulido del flujo de crear causa** (26 jul, feedback de Gastón): fecha de cierre solo por calendario (iOS `display="inline"`, Android diálogo nativo; el spinner en Modal renderizaba vacío con la nueva arquitectura de RN) lo que elimina fechas inválidas; `minimumDate` fijo al montar (antes se recalculaba en cada render); monto y cierre en filas separadas con chips de monto sugerido editables; cobro con un solo campo "Alias o CBU" (método inferido: 22 dígitos = cbu); "volver" en cobro/review apunta al paso anterior del flujo (antes caía al tab activo).
- [x] Limpieza: eliminado todo el scaffold muerto del template de Expo (`app-tabs`, `themed-text/view`, `collapsible`, `animated-icon`, `use-theme`, `use-color-scheme`, `constants/theme.ts`) que nunca se conectó a la app real. `npx tsc --noEmit` corre sin errores.
- [x] **Curador y donante ya no comparten navegación** (26 jul): si la cuenta es curador, la app entera es el panel de curaduría (`src/screens/curar-screen.tsx`, ya no vive en `src/app/`, se decide en `_layout.tsx` antes de montar cualquier tab). "Cerrar sesión" (nuevo, `signOut` en el store) vuelve a una sesión anónima limpia para probar como donante. Perfil también tiene "Cerrar sesión" junto al mail vinculado.
- [x] **Modal de confirmación al enviar una causa a revisión** (26 jul): felicitación, frase, SLA (24 hs hábiles) y botón directo al estado de la causa. Antes navegaba al feed en silencio, sensación de "no pasó nada".
- [x] **Ranking mensual + puntos** (26-27 jul): fórmula núcleo del paper (+50 por aporte, +1 cada $1.000 tope 150/causa, +100 por causa completada). Solo entran donantes con mail vinculado (`profiles.is_registered` + `display_name`). Racha x1,2 y +200 identidad quedan para después.
- [x] **Mercado Pago sandbox** (27 jul): el botón de donar abre el checkout real de MP en modo sandbox (tarjetas de prueba, sin plata real). Edge Function `mp-create-preference` (Deno, deployada por Gastón en el dashboard, secreto `MP_ACCESS_TOKEN` con el token de PRUEBA). `src/lib/mercadopago.ts` + wiring en `donate/[id].tsx` con `expo-web-browser`. ANDA: probado con un usuario de prueba comprador. IMPORTANTE: es un demo CUSTODIAL (cobra a la cuenta dueña del token = donAR, NO al alias del beneficiado). El alias/CBU declarado solo se guarda y se le muestra al curador, no está conectado al pago. Para que sea no-custodial hace falta split de pagos (ver backlog, parqueado + legal).
- [ ] Actividad (placeholder, dice "Próximamente"). Decisión de Gastón: se deja TBD, valor post-MVP.
- [x] **Épica 3: confirmación de transferencia (27 jul).** En el panel de "mi causa" (`cause/[id]` vista de dueño), sección "Transferencias por confirmar": el beneficiado ve cada transferencia pendiente con su comprobante (URL firmada vía `getReceiptUrl`) y confirma ("Me llegó") o rechaza ("No me llegó"). `reviewTransfer(id, approve)` en el store cambia el status a `approved`/`rejected`. Al confirmar, el aporte suma a la meta y cuenta para los puntos automáticamente (todo se deriva de los `approved`, no hay escritura de puntos aparte). La lista principal "Aportes recibidos" ahora solo muestra `approved`.
- [x] **3.3, timeout de 48hs (29 jul, sesión 18).** Si el beneficiado no confirma ni rechaza una transferencia en 48 hs corridas, se auto-confirma sola (`autoConfirmIfNeeded` en el store: mismo patrón lazy client-side que el cierre de causa, evaluado en `getContributions`/`getPendingTransfersForMyCauses`/`getMyActivity`, sin cron ni Edge Function porque Expo Go no las corre). Countdown visible en los tres lugares (`hoursUntilAutoConfirm`, exportado del store).
- [x] **3.4, kudos al donante (29 jul, sesión 18).** Columna `confirmed_at` en `contributions` (antes solo había `created_at`). Actividad y "Lo que donaste" resaltan "✓ Te lo confirmaron" cuando la confirmación fue real (hubo espera) y reciente (últimos 3 días).
- [x] **3.5, escalabilidad de listas (29 jul, sesión 18).** "Transferencias por confirmar" pasó de tarjeta completa (con TODOS los comprobantes precargados) a fila compacta + botones ✓/✕ inline; el comprobante se ve recién al tocar (modal, carga lazy). "Aportes recibidos" se muestra de a 15 con "ver más" en vez de la lista entera siempre.
- [x] **Épica 2: dos métodos de pago (27 jul).** Bifurcación en `donate/[id]`: "Pagar con Mercado Pago" o "Transferir yo mismo". Flujo de transferencia (`transfer/[id].tsx`): muestra el alias del beneficiado (copiable, RLS abierta para causas publicadas), el donante sube comprobante (bucket privado `transfer-receipts`), el aporte entra `pending` (no cuenta para la meta ni puntos hasta confirmarse; `causes_public` solo suma `approved`). Nuevas cols en `contributions`: `method` ('mp'/'transfer'), `receipt_url`. La CONFIRMACIÓN del beneficiado es la Épica 3 (próxima). En la lista de aportes las pendientes se tagean "por confirmar".
- [x] **Panel de "mi causa" (Épica 1 del backlog, 27 jul):** el detalle de una causa propia publicada muestra la vista del creador (recaudado/meta, aportes recibidos, botón "Compartir mi causa" en vez de "Donar a vos mismo"), branch `isOwner` en `cause/[id].tsx`. Perfil tiene sección "Mis causas" con pill de estado, cada fila lleva al panel.
- [x] **Fix bug foto de portada (27 jul):** el view `causes_public` usaba `select c.*`, que Postgres congela a las columnas del momento de crear el view; `image_urls` (y `review_note`/`reviewed_at`) se agregaron después y nunca se exponían. Fix = drop + recreate del view (migración SQL, Gastón la corre). Solo SQL, `mapRow` ya leía `image_urls`.
- [x] **Épica 4, cierre de causa completa (29 jul, sesión 18).** Se dispara por monto (confirmado ≥ meta → `completed`, corta donaciones nuevas) o por tiempo (venció el plazo sin llegar → `closed`); evaluado lazy client-side en `fetchCauses` (`autoCloseIfNeeded`), sin servidor. Reemplaza la regla vieja "meta = objetivo, no techo" del 24 jul. Mini-reporte (monto, personas, días) inline en `cause/[id].tsx` cuando la causa cerró. Agradecimiento: mensaje de cierre público (texto + foto, bucket `cause-covers`) y agradecimiento puntual por aporte (tabla `cause_thanks`); el donante lo ve en Actividad ("Te agradecieron") y en "Lo que donaste". Sin push (bloqueado en Expo Go), como estaba previsto. Excedente sobre la meta: resuelto solo, va al beneficiado sin custodia ni pregunta.
- [x] **Épica 5, puntos/ranking coherentes (29 jul, sesión 18).** Bug real (no solo el matiz que decía el backlog): `getMonthlyRanking` no filtraba `status` en absoluto, un aporte `pending` (¡y hasta `rejected`!) ya sumaba puntos. Arreglado con `.eq('status', 'approved')`. Sumado 5.4: foto de perfil en la fila del ranking (antes solo iniciales) + mini-perfil "estilo Airbnb" al tocar a un donante (avatar, nivel, "se sumó en...", monto donado, causas apoyadas, metas cumplidas, medallas), armado con datos ya públicos por RLS (`profiles` + `contributions`). Extraído `src/lib/gamification.ts` (`levelFor`, `medalsFor`) para no duplicar la fórmula entre Perfil propio y mini-perfil ajeno.
- [x] **Épica 6, monto mínimo (29 jul, sesión 18).** $5.000 ARS, validado en `create.tsx`. Confirmado como falta real: Gastón probó crear una causa con meta de $50 y no lo frenaba.
- [x] **Épica 10, identidad (29 jul, sesión 18).** Nombre y Apellido como DOS campos separados en el registro (no uno solo: garantiza poder sacar las dos iniciales reales, tipo "GP"), guardados como `display_name`/`full_name` real (aprovecha el trigger `handle_new_user()` que ya sabía leer `full_name`, estaba sin usar). Nuevo helper `initialsFor` (`donar-theme.ts`) reemplaza los `.charAt(0)`/`.slice(0,2)` sueltos en feed/Perfil/ranking. Foto de perfil (bucket `avatars`, mismo patrón que `cause-covers`), tapeable desde Perfil. "Cerrar sesión" movido al final de la pantalla de Perfil (antes un link chico arriba junto al mail), benchmark Instagram/Uber/Airbnb: cuenta arriba, salir es la última acción. Recuperar contraseña: flujo completo (deep link `donar://reset-password`, `_layout.tsx` detecta los tokens de recuperación, pantalla nueva `reset-password-screen.tsx`), pero bloqueado para probar hasta configurar dominio en Resend + agregar la redirect URL a la allowlist de Supabase Auth.
- [x] **1.5, propia causa fuera del feed (29 jul, sesión 18, "regla Instagram").** `index.tsx` excluye `mine` de las listas `active`/`completed` del feed general: el feed es para descubrir causas de otros, la propia vive en "Mis causas" (Perfil). "Tus causas" arriba del feed sigue mostrando el trámite en curso. Estado vacío distingue "no hay causas" de "la única que hay es la tuya" (evita que parezca roto probando en solitario).
- [x] **Fix bug "Perfil se recarga sola" (29 jul, sesión 18).** `profile.tsx` y `ranking.tsx` hacían `setLoading(true)` en cada re-foco de la tab (no solo la primera vez), reemplazando toda la pantalla por un spinner de golpe cada vez que se volvía ahí. `useRef` (`hasLoadedOnce`) deja el spinner de pantalla completa solo para la primera carga; los re-focos siguientes refrescan en silencio (mismo patrón que ya usaba bien `activity.tsx`).
- [ ] Reenvío con edición real tras "pedido de info": hoy el botón "Reenviar a revisión" solo cambia el estado, no permite editar los datos de la causa antes de reenviar. Simplificación consciente, documentada como deuda.

### Bugs abiertos
Ninguno crítico conocido al 29 jul (sesión 18). El historial completo de bugs encontrados y resueltos vive en `docs/BACKLOG.md` (sección "Bugs / known issues"). Pendiente sin resolver: confirmar si la barra plana que vio el hermano de Gastón en Android era bundle viejo en caché o algo real (falta que recargue del todo la app y vuelva a probar).

### Estructura del repo (resumen)
`src/app/` rutas Expo Router: `index` (feed), `cause/[id]`, `donate/[id]`, `create`, `cobro`, `review`, `gracias`, `activity`/`ranking` (placeholders), `profile`. `_layout.tsx` decide antes de montar nada si la cuenta es curador; si lo es, renderiza `src/screens/curar-screen.tsx` directo (no es una ruta, es el modo completo de la app para esa identidad, sin tabs). `src/components/` (cause-card, donar-tab-bar, placeholder, ui/). `src/constants/donar-theme.ts` (única fuente de theme; `constants/theme.ts` viejo del scaffold, eliminado). `src/lib/supabase.ts` (cliente + auth: sesión anónima, vincular/loguear con mail y código, `signOut`, evidencia a Storage). `src/store/causes-store.tsx` (toda la data layer: causas, donaciones, actividad, curaduría, `isCurator`/`signOut`). `supabase/schema.sql` (esquema completo + migraciones al final, fecha por fecha). `scripts/seed-demo-cause.mjs` (siembra una causa que no es de quien lo corre, útil para probar donación sin que cuente como "recibido" propio).

### Infraestructura externa (fuera del repo, config manual en dashboards)
- **Supabase**: proyecto `fyaxvofpqqlvtudmnmxi`. Auth con sesiones anónimas por default, ascendidas a cuenta real por mail. Storage: bucket privado `cause-evidence` (RLS: solo dueño de la causa y curador leen). SQL Editor es la única vía de migración (no hay CLI linkeado ni service role key en el repo).
- **Resend**: SMTP custom, configurado en su momento para el código-por-mail. **Desde el 29 jul (login por password) ya NO es crítico para el flujo de auth:** el registro/login con mail+contraseña no manda mails. Resend sigue haciendo falta solo si se quiere "reset de contraseña" o confirmación de mail, y para eso hace falta verificar un dominio propio (el sender de prueba `onboarding@resend.dev` solo entrega al mail de la cuenta de Gastón). Por eso el paso manual en Supabase es apagar "Confirm email".
- **Mercado Pago**: app de desarrollador de Gastón, tipo Checkout Pro. Edge Function `mp-create-preference` deployada en el dashboard de Supabase (con `Deno.serve` nativo, NO el import de deno.land/std que falla al bundlear), secreto `MP_ACCESS_TOKEN` = token de PRUEBA (pestaña "Prueba" del panel; los tokens de prueba hoy empiezan con `APP_USR-`, MP retiró el prefijo `TEST-`). Para probar en sandbox hay que crear un "usuario de prueba comprador" en el panel de MP y loguearse con ESE en el checkout, no con la cuenta real (si mezclás real + preferencia de prueba, MP frena con "una de las partes es de prueba"). Tarjeta de test: `5031 7557 3453 0604`, titular `APRO` para aprobar. Nota: el checkout de MP es quisquilloso con las back_urls en apps; el retorno post-pago puede necesitar ajuste (no confirmado end-to-end que vuelva y registre la donación).

### Versión de Expo (resuelto)
Usar Expo SDK 54, confirmado por Gastón.

---

## 4. Decisiones tomadas (con su porqué)

| Decisión | Qué se decidió | Por qué / qué se resigna |
|---|---|---|
| Flujo del dinero | Híbrido por etapas. MVP: la app conecta, no custodia (transferencia con comprobante o gateway que liquida directo al beneficiado). Etapa 2: custodia con retención automática del fee | MVP liviano en lo legal, valida demanda sin ser fintech regulada. Se resigna control sobre el dinero; se compensa con verificación y trazabilidad |
| Modelo de negocio | Fee = porcentaje sobre el total recaudado por causa que cierra. % exacto por definir, techo de percepción tolerable ~5% (referencia Maratea) | Alinea negocio con éxito de la causa. Se resigna: rinde poco hasta tener escala |
| Gamificación | MVP por reconocimiento no monetario (niveles, medallas, rachas, historial). Beneficios materiales de sponsors = fase 2 | Sin incentivo perverso; permite medir si vuelven por la causa o por el premio |
| Antifraude MVP | Curaduría manual: un curador humano aprueba/rechaza cada causa antes de publicar. Implementado en código el 26 jul: máquina de estados `review` → `needs_info`/`rejected`/`active`, con evidencia (DNI+selfie+documento) en Storage privado | A baja escala, ser el cuello de botella es fortaleza. Un solo curador marcado a mano (`profiles.is_curator`), sin sistema de roles: alcanza para uno |
| Identidad del donante | **LOGIN OBLIGATORIO con mail + contraseña (29 jul, cambio de modelo).** Se abandonó el modelo "anónimo por default + vincular mail opcional". Ahora la app exige cuenta para TODO (donar, crear, recibir); no hay sesión anónima. `ensureSession` ya no crea anónimas, devuelve `null` si no hay cuenta real → `_layout` muestra `AuthScreen`. Requiere apagar "Confirm email" en Supabase (el mail de confirmación no se entrega sin dominio en Resend) | El modelo anónimo era la raíz de todos los líos de identidad: causas huérfanas (dueño anónimo perdido tras `signOut`), donaciones invisibles al cambiar de sesión, sesiones cruzadas. Gastón lo pidió explícito: "tiene que loguearse sí o sí para donar, ni hablar para recibir ni para crear causa; todo tiene que persistir en una cuenta". Se resigna fricción de conversión del donante (dato honesto: pedir cuenta para donar baja conversión, GoFundMe deja donar de invitado), aceptable porque la verificación es el eje de donAR. Se eligió password sobre código-por-mail porque el password NO depende de Resend (se prueba YA con cualquier mail); reset de contraseña queda pendiente (necesita mail) |
| Plataforma | App móvil (Android + iOS) + landing web | El público vive en el celular; la landing da credibilidad y permite compartir |
| Verificación: identidad | DNI (frente/dorso) + selfie con DNI, revisión manual del curador, archivos en Storage privado | Suficiente y barato a baja escala. Servicio externo (Renaper/biometría) = fase 2. Deuda: comparar a ojo no frena un documento falso bien hecho |
| Verificación: SLA | 24 hs hábiles para revisar una causa | Buena experiencia para necesidades urgentes. Primer número que presiona al escalar |
| Verificación: destino del dinero | Al CBU/alias del beneficiado. Curador ve el alias/método en `/curar` para verificarlo | Simple y encaja con "conectar sin custodiar" |
| Verificación: estados de causa | Borrador → En revisión → (Necesita info) → Publicada / Rechazada → Cerrada | "Necesita info" evita rechazar causas legítimas por un papel faltante |
| Verificación: qué juzga el curador | Solo verdad (identidad real, necesidad respaldada por doc de tercero, destino verificable). NO juzga si la causa "merece" | Evita el riesgo ético de jerarquizar dolores |
| Stack | Expo SDK 54 + React Native + Expo Router + TypeScript. StyleSheet con theme central (`src/constants/donar-theme.ts`). Backend: Supabase (DB + Auth + Storage). SMTP: Resend. Pagos: Mercado Pago (no integrado aún) | Un solo código iOS/Android, se prueba en celular con Expo Go sin cuenta Apple ni build nativo. Menos dependencias = menos mantenimiento |
| Google Sign-In | Evaluado y descartado por ahora | Requiere build nativo (EAS dev client) con credenciales OAuth, incompatible con correr todo por Expo Go. Se retoma cuando el proyecto pase a build nativo (va a pasar antes de subir a las stores) |
| Curador vs. donante | Por default, si `is_curator`, la app entera es el panel de curaduría. Desde el 29 jul: se puede alternar SIN cerrar sesión con "Ver como donante" (en el panel de curador) y "Volver a curar" (en Perfil), estado `viewAsDonor` en el store. `signOut` sigue existiendo para arrancar con una identidad totalmente distinta | Caso real: la misma persona puede ser curador Y dueño/beneficiado de una causa propia. El diseño original (mutuamente excluyente, solo `signOut`) la dejaba sin forma de ver sus propias donaciones recibidas sin perder la sesión de curador |
| Subida de evidencia a Storage | `fetch(uri).arrayBuffer()` en vez de `expo-file-system` (API `File`, nueva en SDK 54) | La API nueva fallaba en silencio y dejaba "Enviar a revisión" sin reaccionar. `fetch` + `arrayBuffer` es el patrón más probado para RN + Supabase Storage |
| **Confianza del dinero sin custodia** (27 jul, DECISIÓN CLAVE) | Modelo antifraude del pago por transferencia: (1) transparencia (comprobante público), (2) el BENEFICIADO confirma que le llegó (es el único que ve la plata; sus incentivos alinean con la verdad), (3) identidad verificada. Regla de oro: **solo lo confirmado suma** a la meta/puntos/kudos. Documentado en el paper, sección 11.b | donAR NO está en el camino de la plata (ese es el punto de "no custodiar"), así que nunca puede PROBAR que llegó. El modelo es transparencia + confirmación + identidad, no prueba criptográfica. El hueco (colusión para inflar) es vanidad, no robo; lo encarecen curaduría + identidad. El MVP hace el fraude caro y visible, no lo elimina |
| **Dos métodos de pago** (27 jul) | El donante elige: (a) Mercado Pago, (b) Transferencia con comprobante. La transferencia es el flujo canónico del MVP (no-custodial, el del paper). MP como está es un demo custodial hasta tener split de pagos | Detalle y secuencia de construcción en `docs/BACKLOG.md` |
| **Mercado Pago oculto del todo** (29 jul) | En vez de "próximamente", el botón de MP directamente no se muestra mientras no exista split de pagos + OK legal | Un solo camino (transferencia) es más claro para validar, sin falsa expectativa ni confusión custodial |
| **Timeout de confirmación de transferencia** (29 jul) | Auto-confirmación a las 48 hs corridas si el beneficiado no confirma ni rechaza, con countdown visible en ambos lados (patrón buyer-protection: Mercado Libre/AliExpress) | El comprobante subido es la red de seguridad; el donante no queda a merced de la inacción del beneficiado. Matiz: el principio pasa de "solo lo confirmado suma" a "confirmado explícito o auto-confirmado al vencer" |
| **Cierre de causa por monto** (29 jul, reemplaza "meta = objetivo, no techo" del 24 jul) | Llegar a la meta cierra la causa de verdad (corta donaciones nuevas), no solo la marca "cumplida" siguiendo abierta | Sin esto las causas no tienen fin (palabras de Gastón). Los aportes ya en vuelo se completan igual aunque pasen un poco la meta |
| Monto mínimo para pedir | **$5.000 ARS (29 jul).** Confirmado como falta real: una causa con meta de $50 no se frenaba antes de este fix | Evita ruido y pedidos no serios |
| **Nombre y apellido como campos separados** (29 jul) | En vez de un solo campo de texto libre, el registro pide Nombre y Apellido por separado | Un campo único no garantiza dos palabras (alguien podía poner solo el nombre); con dos campos obligatorios siempre se puede sacar la inicial real de cada uno ("GP") |
| **Encuadre legal + cómo se cobra sin custodiar** (27 jul, investigado en fuentes BCRA/CNV) | El régimen PSPCP del BCRA se dispara por TENER fondos de terceros (cuentas de pago con saldo), no por conectar. Si donAR nunca guarda saldo, lectura más fuerte = afuera. Se cobra sin custodiar vía SPLIT DE PAGOS de MP (la pasarela le manda a donAR solo su comisión propia). Donaciones = zona gris (fuera del crowdfunding CNV, que es de inversión). Riesgo real subestimado: lavado/UIF; identidad+trazabilidad son la defensa. Documentado en paper 11.c + README. NO es asesoramiento legal, falta abogado | Es el supuesto más caro. El régimen se endurece (Com. A 8432/2026). La consulta legal es la primera ficha a mover |
| **MVP de pago = transferencia + aporte voluntario** (27 jul, idea de Gastón) | MVP solo con transferencia directa (no-custodial, sin comisión → afuera de PSP) + "aporte voluntario a donAR" en la pantalla post-transferencia (modelo GoFundMe: comisión 0%, propina opcional del donante). Doble propósito: legal limpio + experimento de willingness-to-pay ("qué tan solidarios son"). MP apagado con flag `MP_ENABLED=false` en `donate/[id]`, NO borrado. Tabla `platform_support` registra el auto-reporte "ya aporté" | Recibir comisión/aporte propio ≠ custodiar plata ajena. El split (para cobrar sin custodiar en MP) queda para cuando haya OK legal |

---

## 5. Supuestos abiertos (a validar, NO confirmados)

- **Regulatorio (el más caro):** que "conectar sin custodiar" no configure intermediación financiera regulada en Argentina (BCRA / registro PSP). A confirmar con abogado.
- **Fee:** el porcentaje inicial y su percepción con usuarios reales. Sin testear.
- **Nomenclatura:** "beneficiado" / "benefactor" son provisorios; testear nombres que no estigmaticen a quien pide antes de la UI.
- **Lista de causas vetadas:** propuesta (políticas/partidarias, deudas/juego, emprendimientos con fin de lucro, salud sin respaldo médico). Falta OK final de Gastón.
- **Umbral "monto alto" en salud** que dispara pago directo al proveedor: sin definir.
- **Detección de duplicados:** hoy a ojo del curador; falta criterio concreto.
- **Nativo vs. web:** si el volumen inicial se sostiene con web responsive, se posterga el desarrollo nativo.
- **Reenvío tras "pedido de info" sin edición real:** el creador no puede editar los datos de la causa antes de reenviar, solo confirma que ya resolvió lo que se le pidió (por fuera de la app). Deuda a resolver si se prueba con gente real y se vuelve un problema.
- **Resend sin dominio propio (RESUELTO como bloqueante identificado, sigue sin resolverse en sí):** el flujo de recuperar contraseña (10.2) ya está codeado de punta a punta, pero no se puede probar con un mail real que no sea el de Gastón hasta verificar un dominio propio en Resend + agregar `donar://reset-password` a la allowlist de Redirect URLs en Supabase Auth. Dos pasos manuales pendientes, ninguno de código.

---

## 6. Hipótesis del MVP y cómo se miden

- **H1 (demanda del que pide):** causas creadas y % que completa la verificación en las primeras semanas.
- **H2 (demanda del que da):** conversión de visitante a donante, y % de causas que alcanzan la meta antes del cierre.
- **H3 (gamificación retiene por la causa):** % de donantes que vuelven a aportar a una segunda causa (sin premios materiales en MVP).

---

## 7. Alcance del MVP (recordatorio)

Dentro: (1) publicar causa, (2) verificación por curaduría manual, (3) donar y registrar aporte, (4) trazabilidad visible, (5) gamificación por reconocimiento.

Fuera por ahora: custodia de fondos y regulación asociada, beneficios materiales/sponsors, verificación automática a escala, ranking público competitivo, retiros/wallets/saldo interno, integración real de pago (Mercado Pago), edición de causa al reenviar tras pedido de info.

---

## 8. Próximos pasos

> **⚡ `docs/PLAN-SONNET-2.md` (31 jul), en ejecución.** Cubre: onboarding con **dos tours** (uno antes del registro con la propuesta de valor, otro después del login iluminando secciones, los dos re-visitables desde "Cómo funciona"), **estados vacíos** unificados, **skeletons** de carga, y la **deuda de Supabase**. **Bloques B (estados vacíos), C (skeletons), D (Tour 1) y F ("Cómo funciona") YA HECHOS** (sesión 21, 31 jul, commits `29e3b22`/`64d8e69`/`74a6724`/`2ffd2b2`).
>
> **Pendiente real del Tour 1: reemplazar el placeholder de imagen por capturas reales de la app** (Épica 13.2, se solapan con los screenshots de las stores). Gastón pidió avanzar el tour ya, sin esperar las capturas; el placeholder está en `welcome-tour-screen.tsx` (`imagePlaceholder`), mismo tamaño final, así que cuando lleguen las fotos es reemplazar ese bloque por `Image`, sin tocar el resto del layout.
>
> **Sigue: Bloque E (Tour 2, coach marks sobre la app real).** Es el bloque técnicamente más frágil, el propio plan dice "hacelo último". Cuando se construya, `como-funciona.tsx` necesita una segunda fila ("Ver el recorrido por la app", ya dejada comentada) que dispare `setReplayTour('app')`, y el nuevo componente del Tour 2 tiene que aceptar el mismo prop `onClose` que ya usa `WelcomeTourScreen`.
>
> **Deuda chica pendiente, no urgente:** `.expo/types/router.d.ts` (generado, gitignored) no se regenera con `npx expo export`, solo con `npx expo start` corriendo de verdad. Por eso `tsc` va a marcar `/como-funciona` como ruta no tipada hasta la primera vez que se levante el dev server. No es un bug de código, se resuelve solo.

**El backlog vivo y priorizado vive en `docs/BACKLOG.md`.** Estado al 30 jul (sesión 19): Épicas 1 (incluye 1.5, más **1.6 nueva sin diseñar**: editar causa), 2, 3, 4, 5, 6, 10.3, 13 (incluye 13.4/13.5), 14 (go-to-market) y **15.1-15.4 (RLS) corridas por Gastón**. 10.2 (recuperar contraseña) sigue bloqueada por configuración externa. Documentos legales/seguridad nuevos (`ciberseguridad.md`, `privacidad.md`, `terminos-y-condiciones.md`) con texto completo, ninguno revisado por abogado.

**Primero que nada al retomar (arranque de la próxima sesión):**
1. **Confirmar en caliente los 2 exploits críticos de RLS** (Épica 15.1/15.2): intentar `update profiles set is_curator = true` sobre la propia cuenta e insertar una `contribution` con `status: 'approved'` fuera del flujo autorizado; ambos deberían fallar ahora. Sin esto, no está confirmado que el fix corrido realmente cierre el hueco.
2. **Decisión de dominio pendiente** (Gastón la va a pensar): `donar-online.com` choca con el competidor real "Donar Online" (`donaronline.org`). No asumir ningún dominio hasta que él confirme.
3. **Diseñar "editar causa" (Épica 1.6)** antes de tocar código: qué campos son editables, si una edición relevante debería re-pasar por curaduría, si se guarda historial.
4. Probar en el celular los cambios visuales de hoy sin verificar en dispositivo real: causa sin foto (letra grande translúcida), estados vacíos con el símbolo de marca, avatar del donante en Actividad/Aportes/Recibidos, montos "$X Mill".
5. **(31 jul)** Probar en el celular lo de la sesión 20: editar el nombre desde el Perfil (10.4) y ver si el encabezado con tu nombre real convence más que el "Tu impacto" que había antes.
6. **(31 jul) Volcar a `supabase/schema.sql` el SQL de los fixes de RLS de la Épica 15.** Están corridos en la base pero no en el repo, así que hoy `schema.sql` recrearía un entorno SIN los cuatro fixes (dos de ellos críticos). Hay que exportarlo de la base real, no reescribirlo de memoria.

Fuera del código, lo que sigue bloqueando la etapa 2:
1. Confirmar con abogado el encuadre regulatorio de "conectar sin custodiar" **y revisar `privacidad.md`/`terminos-y-condiciones.md`** (ya redactados, listos para esa revisión). Es el supuesto que más puede cambiar el modelo.
2. Definir el fee inicial y testear su percepción.
3. OK final de Gastón sobre la lista de causas vetadas.
4. Validar H1/H2/H3 con gente real (todavía sin testear con usuarios). Ver plan de fase 0/1 en `docs/go-to-market.md`.
5. Pulir la historia con IA al crear una causa. DECISIÓN 26 jul: pospuesto. Costo por uso es casi nulo (Haiku 4.5, fracción de centavo por historia); lo caro es la infraestructura: NO se puede llamar a la API de Anthropic directo desde la app (expondría la clave), hay que interponer una Supabase Edge Function que guarde la API key como secreto, y Gastón necesita cuenta propia en Anthropic con facturación. Se retoma cuando él lo decida.
6. Evaluar si conviene edición real en el reenvío tras "pedido de info", o si alcanza con el flujo actual (reenviar tal cual) para esta etapa de pruebas. Se solapa con la Épica 1.6 nueva (editar causa en general): conviene resolverlas juntas.
7. **Verificar un dominio propio en Resend** (bloquea 10.2 de punta a punta) + agregar `donar://reset-password` a la allowlist de Redirect URLs en Supabase Auth (Authentication > URL Configuration). Ninguno de los dos es código, son pasos manuales de Gastón en los dashboards.
8. Integración real de pago (Mercado Pago) cuando el flujo esté validado con gente real.
9. Correr en Supabase (SQL Editor) las migraciones nuevas de la sesión 18 si todavía no se corrieron: `closed_at`/`closing_message`/`closing_photo_url` en `causes` + tabla `cause_thanks`; `confirmed_at` en `contributions`; `avatar_url` en `profiles` + bucket `avatars`. Todas están al final de `supabase/schema.sql`, fechadas 29 jul.
10. Pasar Supabase de free tier a un plan pago antes de anunciar el lanzamiento (ver checklist completo en `docs/ciberseguridad.md`).
11. Completar los campos entre corchetes de `privacidad.md`/`terminos-y-condiciones.md` (razón social, mail de contacto) y publicarlos en una URL real antes de cargar la ficha de Play Store.

---

## 9. Registro de sesiones (log)

### 31 jul 2026 (sesión 21, con Sonnet: Bloques B y C de `docs/PLAN-SONNET-2.md`)
- **Pedido de Gastón:** ejecutar el Bloque B (estados vacíos) y después el C (skeletons) del plan escrito en la sesión 20, un commit por bloque.
- **Bloque B, HECHO.** Componente nuevo `src/components/empty-state.tsx` (`title`, `subtitle?`, `actionLabel?`, `onAction?`, `BrandMark` adentro), copiado del layout del feed (la referencia que el plan marcaba como ya bien resuelta). Reemplazados los 5 estados vacíos existentes (feed, `donated.tsx`, `received.tsx`, `ranking.tsx`, `activity.tsx`) sin tocar el copy: donde el texto original era una sola oración se usó como `title` sin `subtitle`; donde tenía dos oraciones naturales se partió en `title`/`subtitle` en el punto, palabra por palabra igual. Sumado `BrandMark` donde faltaba (ranking y actividad tenían emoji suelto, 🏅/🔔) y CTA donde había una acción siguiente obvia: `received.tsx` sumó "Crear una causa" → `/create` (antes no tenía ninguna, el plan lo marcaba como faltante); `donated.tsx` **ya tenía** su CTA "Ver causas" de una sesión anterior (el inventario del plan estaba desactualizado en ese punto), se migró igual al componente nuevo. **Estado vacío nuevo de "Mis causas" en el Perfil:** sacado el `myCauses.length > 0 &&` que escondía la sección entera; ahora una cuenta nueva ve "Todavía no creaste ninguna causa" + el mismo mensaje de curaduría que el Tour 1 va a repetir + CTA a `/create`. Commit `29e3b22`.
- **Bloque C, HECHO.** Componente `src/components/skeleton.tsx`: primitivo `Skeleton` (bloque `Colors.line`, pulso de opacidad con `Animated.loop` nativo de RN, `useNativeDriver: true`, sin `react-native-reanimated` por la razón ya documentada en `tab-bar-scroll.tsx`) más siluetas compuestas por pantalla: `FeedSkeleton` (2 tarjetas fantasma con la forma de `CauseCard`: portada, título, barra, montos), `ProfileSkeleton` (avatar + nombre + pill + 2 stat cards), `RankingSkeleton` (5 filas círculo + 2 líneas) y `ActivitySkeleton` (4 filas círculo + 2 líneas, mismas medidas que las filas reales). Reemplazan los spinners de pantalla completa **en el mismo punto donde vivían**, sin tocar el patrón `hasLoadedOnce` de `profile.tsx`/`ranking.tsx` (el skeleton cuelga de la misma condición `loading`, que solo se prende en la primera carga). `activity.tsx` no tenía ese patrón y no hacía falta agregarlo: `data` nunca vuelve a `null` después del primer fetch, así que el skeleton solo se ve una vez, por construcción. El feed no tiene el problema en absoluto (su `loading` es el de sesión, no se re-dispara por refoco de tab). Commit `64d8e69`.
- **Limpieza de paso:** en cada archivo tocado se sacaron los estilos y imports que quedaron muertos al reemplazar el contenido (`ActivityIndicator` sin más usos en `ranking.tsx`/`activity.tsx`, estilos `empty*`/`center` sin más referencias).
- **Verificación en cada bloque:** `npx tsc --noEmit` limpio, `npx expo lint` con los mismos 2 warnings preexistentes de siempre (`Text` sin usar en `donar-tab-bar.tsx`, `empty` sin usar en `causes-store.tsx:1183`), `npx expo export --platform ios` bundlea. **No probado en dispositivo real.**

### 31 jul 2026 (sesión 21, cont.: Bloque D, Tour 1, con placeholder de imagen)
- **Pedido de Gastón: "avanza con el tour y deja placeholders".** Rompe a propósito el orden del plan (B → C → capturas → D), que dejaba las imágenes reales como bloqueante antes de tocar el Tour 1. Se avanzó el Bloque D igual, con un placeholder del tamaño final en vez de esperar las capturas, tal como el propio plan preveía como salida ("dejá el layout andando con un placeholder... para no bloquearte").
- **Bloque D, HECHO.** Hook `src/hooks/use-onboarding-flag.ts` (carpeta nueva `src/hooks/`): lee/escribe AsyncStorage con clave versionada, expone `seen`/`loading`/`markSeen`. Pantalla `src/screens/welcome-tour-screen.tsx`: 4 slides con el copy exacto del plan (nada reescrito), `ScrollView` horizontal con `pagingEnabled` + dots + "Siguiente"/"Empezar", "Saltar" fijo arriba a la derecha como **hermano** del `ScrollView` (nunca ancestro, por la trampa ya documentada del 27 jul: un `Pressable` que envuelve el scroll horizontal le compite el gesto). Sin `BlurView`, fondos sólidos. Prop `onClose` (no `onDone`) a propósito: mismo nombre que va a necesitar el Bloque F para reusar la pantalla como replay modal.
- **Enganche en `_layout.tsx`:** gate nuevo entre `passwordRecovery` y `!isAuthenticated`, como pedía el plan. El `loading` del hook se suma al `loading` del store en la condición de la pantalla de carga azul, para que la lectura de AsyncStorage no deje ver un flash del login (o de la app) antes de que el tour tape la pantalla.
- **Placeholder de imagen:** bloque `Colors.skyTint` con borde, alto proporcional (`aspectRatio: 0.82`, tope 360px) pensado para el tamaño final de una captura de teléfono en vertical, con el texto "Imagen de la app" para que quede obvio que es temporal. Cuando lleguen las capturas reales (Épica 13.2), es solo cambiar ese bloque por `Image`, la estructura del slide no se toca.
- **Verificación:** `npx tsc --noEmit` limpio, `npx expo lint` con los mismos 2 warnings preexistentes de siempre (ninguno nuevo), `npx expo export --platform ios` **y** `--platform android` bundlean bien (paridad, no solo iOS). **No probado en dispositivo real todavía**, ni el swipe del carrusel ni que el flag realmente persista entre reaperturas de la app.
- **Sigue:** Bloque F ("Cómo funciona" en el Perfil, deja re-ver el Tour 1 y habilita el Tour 2 más adelante) y después el Bloque E (Tour 2).

### 31 jul 2026 (sesión 21, cont.: Bloque F, "Cómo funciona")
- **Pedido de Gastón:** seguir con el Bloque F después del D.
- **Bloque F, HECHO.** Estado nuevo `replayTour: 'welcome' | 'app' | null` + `setReplayTour` en `causes-store.tsx`, mismo patrón que `viewAsDonor` (estado en memoria, no toca AsyncStorage ni Supabase: es un repaso a pedido, el flag `donar.tour.welcome.v1` sigue en visto). Pantalla nueva `src/app/como-funciona.tsx`, registrada en el Stack de `_layout.tsx` junto a `donated`/`received`, con una fila "Ver la presentación" que prende `replayTour = 'welcome'` y vuelve atrás. `_layout.tsx` renderiza `WelcomeTourScreen` dentro de un `Modal` nativo (mismo componente que usa `react-native` en `profile.tsx` para el editor de nombre) por encima de todo el Stack, cuando `replayTour === 'welcome'`; se cierra igual que la primera vez, llamando al mismo `onClose`.
- **Fila nueva en el Perfil:** "Cómo funciona donAR", justo arriba de "Cerrar sesión" como pedía el plan (el patrón Instagram/Uber/Airbnb ya establecido deja "Cerrar sesión" como última acción).
- **A propósito, NO se agregó la fila del Tour 2 ni los links legales.** El Tour 2 (Bloque E) todavía no existe: mostrar un botón que no hace nada sería peor que no mostrarlo. Queda un comentario en `como-funciona.tsx` marcando dónde va esa fila (dispara `setReplayTour('app')`) cuando se construya. Los links a `terminos-y-condiciones.md`/`privacidad.md` también quedan comentados, como pedía el plan: no tienen URL pública todavía (Épica 15.6/15.7).
- **Verificación:** `npx expo lint` limpio (mismos 2 warnings preexistentes), `npx expo export --platform ios` y `--platform android` bundlean bien. **`npx tsc --noEmit` marca UN error, no de código:** `router.push('/como-funciona')` no tipa porque `.expo/types/router.d.ts` (archivo generado, gitignored) no se regeneró desde el 30 jul; solo lo hace `npx expo start` corriendo de verdad, no `expo export` ni `expo lint`. Se intentó levantar el dev server en background para regenerarlo pero Gastón cortó esa acción; queda pendiente que se resuelva solo la próxima vez que alguien corra `npx expo start` (Metro CLI, no hace falta hacer nada especial). **No probado en dispositivo real.**

### 31 jul 2026 (sesión 20: casing de marca + editar el propio nombre)
- **Contexto del pedido:** Gastón quería commitear algo para mantener la actividad diaria en GitHub. El árbol estaba limpio y todo lo de la sesión 19 ya estaba pusheado, así que en vez de un commit de relleno se buscó en el backlog trabajo real que cerrara sin abrir una discusión de diseño. Se le ofrecieron opciones y eligió: normalizar la marca **incluyendo `app.json`**, y sumar la Épica **10.4** (editar el propio nombre).
- **Casing de la marca: el problema era más grande que la nota del backlog.** La nota de 13.4 decía que el único caso era el wordmark del login. Al ir a arreglarlo, el login ya no tenía ese texto (usa el asset), pero **el copy de la app decía "DonAR" en 8 lugares visibles al usuario**, y la raíz real estaba en `app.json` (`"name": "DonAR"`), que es lo que el SO muestra debajo del ícono y en Ajustes. Por eso los 3 strings de permisos ("Ajustes del celular > DonAR > Fotos") eran *correctos* hasta ahora: coincidían con el sistema. Se le planteó la disyuntiva antes de tocar (cambiar `app.json` afecta el nombre en el home screen y la ficha de las stores) y eligió normalizar todo, aprovechando que todavía no está publicada. Tocado: `app.json` (`name` + `photosPermission`), `review.tsx`, `donate/[id].tsx`, `cause/[id].tsx` (incluidos los **dos textos de compartir**, los que salen a WhatsApp), `cobro.tsx`, `create.tsx`, `profile.tsx`, `donar-theme.ts`. `gracias.tsx` ya venía en minúscula, lo que confirmó cuál era la forma buena. **Pendiente de verificar en el build nativo:** cómo se lee "donAR" en minúscula debajo del ícono.
- **Épica 10.4 HECHA: editar el propio nombre desde el Perfil.** Antes `display_name` se escribía una sola vez (al registrarse) y las cuentas viejas quedaban con el nombre derivado del mail para siempre — el caso de Gastón del 30 jul, que hubo que arreglar con un UPDATE a mano. Implementado con el mismo patrón que la foto: el nombre es tapeable en el encabezado y abre un modal con **Nombre + Apellido separados**, misma validación que el registro (1 carácter mínimo cada uno, unidos por un espacio) para no romper la garantía de dos iniciales de `initialsFor`. Función nueva `updateProfileName` (`src/lib/supabase.ts`): escribe `display_name` + `full_name` en `profiles` y sincroniza la metadata de auth (best-effort). En el store, `updateDisplayName` refleja el valor en el estado para que header e iniciales cambien sin recargar.
- **Hallazgo de paso, vale la pena tenerlo presente:** el encabezado del Perfil decía **"Tu impacto"**, nunca mostraba tu nombre. O sea que un `display_name` feo era invisible desde tu propia app y solo lo veían los demás en el ranking. Ahora muestra el nombre real con un ✎ al lado. **Es el único cambio de esta sesión que altera algo que Gastón había elegido**, así que queda anotado por si lo quiere revertir (una línea en `profile.tsx`).
- **No hizo falta SQL ni policy nueva** para 10.4: `profiles update own` ya permitía el update de la fila propia y el trigger de 15.2 fuerza `is_curator`/`points` a su valor anterior, así que este camino solo toca columnas libres. Verificado leyendo el schema, no asumido.
- ⚠️ **Deuda detectada, NO resuelta: el SQL de los fixes de RLS de la Épica 15 (15.1 a 15.4) no está en `supabase/schema.sql`.** El backlog dice que vive "en la conversación del 30 jul". Los triggers están corridos en la base de Gastón, pero el repo no los tiene: si hubiera que recrear el entorno desde `schema.sql`, saldría **sin los cuatro fixes de seguridad**, incluidos los dos críticos. No se reconstruyó el SQL de memoria a propósito (habría sido inventarlo). Hay que exportarlo de la base real y agregarlo al schema.
- **Verificación:** `npx tsc --noEmit` limpio, `npx expo lint` con los mismos 2 warnings preexistentes de siempre (ninguno nuevo), `npx expo export --platform ios` bundlea bien. **Nada probado en dispositivo real todavía.**

### 31 jul 2026 (sesión 20, cont.: planificación del onboarding, estados vacíos, skeletons y Supabase)
- **Pedido de Gastón:** planificar cómo seguir (lo que falta de Supabase, estados vacíos, skeletons y sobre todo un tour de primer ingreso) y dejarlo escrito **para atacarlo después con Sonnet y ahorrar tokens**. O sea: el entregable era el plan, no el código.
- **Resultado: `docs/PLAN-SONNET-2.md`**, mismo formato de handoff que el `PLAN-SONNET.md` del 29 jul. Seis bloques (A Supabase, B estados vacíos, C skeletons, D Tour 1, E Tour 2, F "Cómo funciona"), cada uno con archivos concretos, patrones del repo a copiar, "done cuando", y una sección de "qué NO hacer". Épicas nuevas **16 (onboarding)** y **17 (estados vacíos)** en el backlog, y 8.6 (skeletons) pasó de "sin diseñar" a planificado.
- **Decisión de producto de Gastón: son DOS tours, no uno.** Le planteé la disyuntiva (antes del registro vs. después del login) y respondió que quiere los dos, porque responden preguntas distintas: (1) **antes del registro**, un modal con pasos e imágenes sobre la propuesta de valor, para que sepa qué es donAR antes de dar sus datos; (2) **después del login**, un recorrido que ilumina secciones para enseñar dónde está cada cosa. Requisito explícito suyo: **los dos se pueden omitir y volver a ver**, desde un lugar fijo tipo "Cómo funciona".
- **El copy de los 4 slides del Tour 1 ya está escrito en el plan**, derivado del pitch que él mismo formuló ("donAR conecta a quien quiere ayudar con quien lo necesita... la confianza es el producto"). El slide 3 (el curador verifica antes de que salga) es el que diferencia a donAR de cualquier otra plataforma: queda marcado como intocable.
- **Cadena de dependencias que ordena todo el trabajo (vale la pena tenerla clara):** el Tour 1 necesita imágenes → se decidió que sean **capturas reales de la app** (Gastón lo pidió así y se solapan con la Épica 13.2, los screenshots de las stores: el mismo trabajo sirve dos veces) → pero una captura con un spinner pelado o un Perfil vacío no vende nada → **entonces los estados vacíos y los skeletons van PRIMERO**, aunque el tour sea lo más importante. Orden final: B → C → capturas → D → F → E.
- **Hallazgos al inventariar el código (no salieron del backlog, salieron de leerlo):**
  - **Falta entero el estado vacío de "Mis causas" en el Perfil:** la sección se esconde con `myCauses.length > 0 &&`, así que una cuenta nueva entra al Perfil y no ve ninguna señal de que puede crear una causa.
  - **Ranking y Actividad no tienen `BrandMark`** en su estado vacío, solo texto; el feed sí y está bien resuelto (es la referencia a copiar).
  - **Corrección sobre la marcha en el plan:** primero escribí que el skeleton usara `react-native-reanimated` (está en `package.json`). Está mal: la Épica 8.2 lo descartó a propósito porque **el proyecto no tiene `babel.config.js`** y no se puede confirmar el plugin de worklets, con `tab-bar-scroll.tsx` de precedente usando el `Animated` nativo de RN. Corregido en el plan y anotado en "qué NO hacer".
- **Sobre el estado del onboarding en la base: no toca Supabase.** Los dos tours guardan su flag en AsyncStorage (ya instalado, ya en uso como storage de la sesión de Supabase) con clave versionada. El Tour 1 **no puede** guardarlo en `profiles` porque corre antes de que exista la cuenta. Consecuencia práctica: **todo el bloque de onboarding está desbloqueado de la deuda de Supabase** y se puede hacer en paralelo.
- **Bloque A (Supabase) es casi todo trabajo manual de Gastón**, no código: volcar al repo el SQL de los triggers de la Épica 15 (con las queries de `pg_get_functiondef`/`pg_get_triggerdef`/`pg_policies` ya escritas en el plan para exportarlo de la base real, en vez de inventarlo), confirmar en caliente los 2 exploits críticos, verificar que las migraciones de la sesión 18 estén corridas, los 2 bloqueantes de 10.2 (Resend + Redirect URLs) y el pase a plan pago antes del lanzamiento.

### 30 jul 2026 (sesión 19, cont.: bug real de identidad del donante + chequeo de gbrain)
- **`https://github.com/garrytan/gbrain` (pregunta de Gastón): no sirve para donAR.** Es un sistema de memoria/grafo de conocimiento para agentes de IA (Garry Tan, CEO de YC), no tiene relación con crowdfunding/pagos/Supabase. Recomendación: no sumarlo al proyecto.
- **Bug real reportado por Gastón: al ver una donación que le hacen, no aparece la foto ni las iniciales del donante, dice "Alguien de la comunidad".** Diagnosticado en el código (no a ciegas): `getContributions`, `getReceivedContributions` y `getPendingTransfersForMyCauses` (`causes-store.tsx`) nunca buscaban el `profiles` real del donante, el nombre estaba hardcodeado a esa frase genérica para cualquier donación no anónima y no propia. En `cause/[id].tsx` era peor: el círculo de avatar mostraba `nombre.slice(0,2)` sobre esa cadena genérica ("AL" de "Alguien"), nunca la persona. **Arreglado:** las tres funciones ahora hacen el mismo join a `profiles` (display_name/avatar_url) que ya usaba el mini-perfil del ranking, respetando `anonymous` (nunca revela nombre/foto si el donante donó anónimo). El avatar de `cause/[id].tsx` ahora muestra la foto real o las iniciales correctas (`initialsFor`). `received.tsx`/`activity.tsx` se arreglan solos (mismo campo `donorName`, sin tocar su JSX) porque ahora el store computa el valor correcto; no se les agregó avatar (no tenían ese slot de diseño, sería un cambio de layout aparte si se pide). `npx tsc --noEmit` y `npx expo lint` limpios. **No commiteado todavía.**

### 30 jul 2026 (sesión 19, cont.: logo en loading, montos abreviados, y el corazón de causa en discusión)
- **Símbolo del logo en la pantalla de carga inicial (Épica 13.4).** Pedido de Gastón: "si nos matamos iterando el logo, ponelo en algún lado donde haga sentido", más "las pantallas de loading también, capaz cuando inicio sesión, sería lindo tenerlo". Diagnosticado: `_layout.tsx` mostraba un `ActivityIndicator` pelado sobre fondo vacío mientras chequea la sesión al abrir, justo después del splash nativo (que sí tiene el logo) — contraste feo, pérdida de marca en esa transición. Fix: símbolo real (`assets/images/android-icon-foreground.png`, ya generado, blanco sobre celeste) arriba del spinner. De paso noté que el login ya tenía el wordmark (consistente con el sistema documentado en `proceso-logo.md`: símbolo solo en el ícono, wordmark adentro de la app), pero con casing distinto ("DonAR" vs. "donAR" del resto de la marca); no lo toqué, es cosmético, anotado en BACKLOG 13.4 para su ronda de ajustes finos.
- **El corazón hardcodeado de cada causa: EN DISCUSIÓN, no resuelto (Épica 13.5).** Confirmé en el código que el emoji 💙 de una causa está hardcodeado (no hay picker), se repite igual en 8 lugares del app. Le señalé que choca con su propia decisión de marca ("salir del corazón genérico", `proceso-logo.md`). Le ofrecí 3 opciones (sacar el emoji y usar color+inicial, categorías con ícono propio, rotar el emoji); ninguna lo convenció. Propuso él mismo usar el símbolo/logo de donAR ahí, estilo LinkedIn con logos de empresa. **Le hice un contraargumento honesto:** LinkedIn en realidad no usa su propio logo para ese placeholder (usa un ícono neutro), y repetir el símbolo de la app en cada fila de causa lo banalizaría (deja de leerse como "sin foto" y empieza a leerse como marca de agua), además de chocar de nuevo con el sistema símbolo-solo-en-el-ícono ya establecido. Alternativa que dejé sobre la mesa: reservar el símbolo para momentos puntuales (loading, ya hecho; estados vacíos) y usar algo neutro para el placeholder de causa. **Pidió conversarlo antes de tocar código: no implementar nada de esto todavía**, seguir la charla la próxima vez que se retome.
- **Montos abreviados en el perfil (pedido de Gastón: "recibí 111.000.000 y en Recibiste pasó a 2 líneas").** Nueva función `formatARSCompact` (`donar-theme.ts`): abrevia desde el millón, estilo Instagram/redes (`$111M`, `$2,2M` con coma decimal a la argentina). Aplicada SOLO en las tarjetas de stat "Donaste"/"Recibiste" del perfil (`profile.tsx`). Deliberadamente no se tocó `formatARS` en ningún otro lado (montos de un aporte individual, "Mis causas", listas de donaciones): ahí la cifra exacta es la trazabilidad, el eje del producto, nunca debe abreviarse.
- **`npx tsc --noEmit` y `npx expo lint` limpios en cada paso** (mismos 2 warnings preexistentes de siempre, no tocados).
- **Cierre de la discusión del ícono de causa: implementada la combinación acordada (Épica 13.5, HECHA).** Símbolo real de marca (`BrandMark`, componente nuevo en `src/components/brand-mark.tsx`, reutilizado ahora en loading + los 3 estados vacíos que antes usaban 💙: feed, "Lo que donaste", "Lo que recibiste"), reservado a momentos puntuales, nunca repetido fila por fila. Ícono neutro (color + inicial del título, función nueva `causeInitial` en `donar-theme.ts`) para el placeholder de causa sin foto en los 6 lugares que sí se repiten por fila (feed, hero del detalle, Mis causas, Lo que donaste, Actividad). De yapa, "Lo que recibiste" (`received.tsx`) pasó a mostrar la foto/iniciales del DONANTE en vez del ícono de causa, mismo criterio que ya se había aplicado en Actividad. Gastón pidió "generá la combinación" tras la charla, así que esto SÍ se implementó (a diferencia del resto de la sesión, no quedó como propuesta abierta).
- **Gastón pidió commitear y subir todo lo pendiente de la sesión.** Se hizo un solo commit con: fix del donante real en Aportes/Recibidos/Actividad (avatar + nombre), símbolo de marca en loading, montos abreviados del perfil, y la combinación completa del ícono de causa (13.5).
- **Corrección sobre la marcha: el fix de montos grandes era incompleto.** Gastón vio el mismo problema ($111.000.000) en una CAUSA CERRADA, no solo en el perfil, y pidió el formato "111 Mill" (con espacio, criollo), no "111M" al estilo redes en inglés, con umbral en $10.000.000 (por debajo no vale la pena). Corregido `formatARSCompact` y extendido a todo monto agregado/resumen: recaudado/meta del feed y del detalle de causa, mini-reporte de cierre, "Mis causas". Se mantiene exacto en: monto de un aporte puntual, y cualquier instrucción de pago/input activo (donar, transferir, revisar en curaduría) — ahí abreviar sería además un riesgo de seguridad (una instrucción de transferencia con "$111 Mill" es ambigua para un banco).
- **Formato de "mill" corregido de nuevo (30 jul).** Tercer intento: quedó mal escrito como "111 Mill" (mayúscula, sin punto); el pedido real era **"111 mill."** (minúscula + punto de abreviación, como redes). Corregido en `formatARSCompact` y en el registro del backlog. Aprendizaje para no repetir: cuando Gastón da un formato de texto exacto entre comillas, copiarlo literal (mayúsculas, puntuación, espacios), no "interpretarlo".
- **Idea volada de monetización, anotada en visión (30 jul, propuesta de Gastón, SIN validar ni construir):** funcionalidades premium de alcance/visibilidad en el feed (destacar una causa) si el producto se complejiza, posible clave de ingreso que no dependa de que una causa cierre. Documentado en el paper fundacional, sección 8, con la tensión ética que trae (choca con "el curador no juzga mérito" y "sin ranking competitivo") y la distinción clave si se retoma: pagar por visibilidad el BENEFACTOR (más limpio) vs. pagar por visibilidad el BENEFICIADO (delicado). Referenciado también en `docs/BACKLOG.md` (Parqueado).
- **Nuevo pedido, importante: poder editar causas.** Hoy no existe ninguna forma de editar una causa después de creada (ni título, ni historia, ni monto, ni fecha, ni fotos). A pedido de Gastón, esto se manda al backlog (Épica 1.6) en vez de implementarse ahora: falta diseñar qué campos son editables, si una edición relevante debería re-pasar por curaduría, y si se guarda historial (mismo espíritu de transparencia del resto del producto). Queda como lo primero a definir la próxima sesión.
- **Copy del mini-perfil de donante partido en 2 líneas por stat** ("puntos"/"este mes", "causas"/"apoyadas", "metas"/"cumplidas"), pedido de Gastón porque no quedaba prolijo en una línea. De paso confirmé que el ranking ya mostraba `display_name`, nunca el mail directamente: si a Gastón le aparecía algo con forma de mail era el dato de SU cuenta vieja (derivado del mail antes de que existiera el campo Nombre/Apellido), no un bug de código. Le pasé el SQL puntual para corregirlo y anoté en el backlog (10.4) que falta una forma real de editar el propio nombre desde la app.
- **Extraído el mini-perfil de donante a un componente reusable** (`src/components/donor-profile-modal.tsx`), a pedido de Gastón: quería la misma vista del ranking (nivel, medallas, causas apoyadas, monto donado) al tocar a un donante en "Aportes recibidos", con un botón de "Agradecer" adentro cuando corresponde. `ranking.tsx` se simplificó bastante usándolo, sin cambiar su comportamiento.
- **Sacada la explicación de más de "Transferencias por confirmar"** ("...o resolvela directo con ✓ / ✕"): los íconos ya están al lado de cada fila, no hacía falta explicarlos en texto.
- **Pantalla de carga inicial: de blanco+badge a azul de marca a pantalla completa**, mismo asset que el splash nativo (`splash-icon.png` sobre `Colors.brand`), para que no haya salto de color entre el splash del sistema y esta pantalla. Pedido de Gastón: "para eso la hicimos" (el splash celeste).
- **Anotado en backlog, sin implementar:** skeletons de carga (8.6, "falta eso", hoy todo es spinner de pantalla completa) y algoritmo de ordenamiento del feed (7.7, sin criterio definido hoy).

### 30 jul 2026 (sesión 19, cont.: barrido de seguridad pre-Play Store)
- **Pedido de Gastón:** cómo funciona el build nativo (aclarado: mismo código, dos binarios vía EAS Build; cuentas de Apple/Google y pago las tiene que crear él, el resto lo puedo correr yo) y un barrido de seguridad de todo el código antes de subir a Play Store.
- **Hallazgo central, gravedad crítica: RLS de Supabase no fuerza las reglas de negocio que hoy solo respeta el código de la app.** Como la key pública es pública por diseño, cualquiera con una cuenta puede pegarle directo a la API saltando la UI. Cuatro agujeros reales, documentados con SQL de fix en **Épica 15 nueva de `docs/BACKLOG.md`**:
  - **15.1 (crítico):** se puede insertar una `contribution` con `status: 'approved'` y monto inventado sin transferir nada. Rompe el eje "solo lo confirmado suma" (11.b del paper) de raíz: infla meta, ranking, y puede disparar el cierre automático de una causa ajena.
  - **15.2 (crítico):** cualquiera puede hacer `update profiles set is_curator = true` sobre sí mismo. Da acceso a leer DNI/selfie ajenos (bucket `cause-evidence`) y a aprobar/rechazar cualquier causa.
  - **15.3 (alto):** el dueño de una causa puede ponerle `status = 'active'` él mismo, sin pasar por curaduría.
  - **15.4 (medio):** el dueño de una causa puede alterar el `amount` de un aporte ajeno ya registrado (la policy de confirmar transferencia no restringe columnas).
  - Menores: deep link de recuperar contraseña con esquema custom (interceptable en Android, no urgente); falta Privacy Policy publicada + formulario de Data Safety de Play Store (no es código, pero bloquea la aprobación igual, dado que la app junta DNI+selfie).
- **Nada de esto se aplicó todavía.** El SQL de fix (policies nuevas + triggers de columnas protegidas) se le dio a Gastón en el chat para pegar en el SQL Editor de Supabase, mismo patrón que toda migración del proyecto (no tengo service role key ni CLI linkeado). **Antes de publicar en Play Store, estos 4 puntos son bloqueantes**, no cosméticos: el producto entero se apoya en la trazabilidad del dinero, y hoy esa trazabilidad se puede falsificar con una llamada de API.
- **Pedido de seguimiento: documentar todo esto en documentos aparte.** Creados **`docs/ciberseguridad.md`** (documento vivo: modelo de amenaza, mapa de datos sensibles por tabla/bucket con quién puede leer qué, registro de las revisiones con severidad y estado, principios de seguridad adoptados —RLS-first, patrón "trigger de columnas protegidas"—, riesgos aceptados a propósito con su porqué, checklist de seguridad pre-Play Store) y **`docs/privacidad.md`** (borrador de política de privacidad en español, armado desde el código real de qué se recolecta hoy, no desde lo que se planea: cuenta, verificación DNI/selfie, causa, donación/comprobante; para qué se usa; con quién se comparte —Supabase, Resend, Mercado Pago—; derechos ARCO vía Ley 25.326/AAIP; marcado explícitamente como no-asesoramiento-legal, con campos entre corchetes para completar antes de publicar: razón social y mail de contacto). Ambos linkeados desde README y BACKLOG. Commiteado y pusheado a `main` (`a47f9f3`).
- **Gastón corrió el SQL de los 4 fixes (15.1-15.4) en el SQL Editor de Supabase, mismo día.** Estado actualizado en `docs/BACKLOG.md` y `docs/ciberseguridad.md` (tabla de revisiones, checklist). **Falta un paso de verificación:** confirmar en caliente que los dos exploits críticos (auto-nombrarse curador, insertar una donación "approved" trucha) ahora fallan de verdad; no se hizo todavía en esta sesión.
- **Pedido: armar Términos y Condiciones.** Creado **`docs/terminos-y-condiciones.md`** (rol de donAR "conecta, no custodia, no garantiza el uso posterior del dinero"; reglas para quien publica una causa y quien dona; gamificación sin valor monetario; conductas prohibidas; límite de responsabilidad; mismo formato y mismos campos entre corchetes pendientes que `privacidad.md`). BACKLOG 15.7 pasa a hecho el borrador.
- **Hallazgo no pedido, importante: el dominio que Gastón dijo tener (`www.donar-online.com`) choca de nombre con un competidor real.** Al chequear (no resuelve DNS, probablemente recién comprado sin configurar) se confirmó por búsqueda web que **"Donar Online" es una organización real que opera hoy en Argentina** (`donaronline.org`, sede en Villa Crespo CABA), justo la que el propio paper fundacional nombra como competidora en la sección 3. `donar-online.com` es casi idéntico a su marca. Riesgo real: confusión, posible conflicto legal, y pérdida del activo de marca "donAR" que arma todo el plan de go-to-market. **Gastón lo dejó en pausa para pensarlo** (no descartó el dominio, tampoco confirmó seguir con él). Anotado en `docs/ciberseguridad.md` (15.8) y referenciado en `docs/go-to-market.md` tarea 14.2. Decisión pendiente, no tomar por sentado ningún dominio hasta que la confirme.

### 30 jul 2026 (sesión 19: capítulo go-to-market, con Fable 5)
- **Pedido de Gastón:** "capítulo aparte" con dos tracks que se le venían cruzando: cómo marketinear/vender donAR. Tres entregables: plan de marketing (jugando con la marca donAR y la llegada desde la emoción), plan de una landing chica de presentación (con imagen 3D de la app, value prop atrapante, que incite a descargar), y plan de financiación (pitch + elevator pitch, "que pegue justo donde se necesita").
- **Entregado: `docs/go-to-market.md` (v0.1)**, escrito con la skill `estilo-gaston`. Lo estructural:
  - **Marketing:** insight raíz "la gente quiere ayudar pero desconfía" (Maratea probó la demanda, la IGJ probó el gap de confianza) → jerarquía de mensaje fija: emoción abre, confianza cierra, reconocimiento retiene. Growth loop nativo como centro del plan: quien crea una causa tiene urgencia real por difundirla, cada causa es una campaña con embajador motivado. Cold start clásico de marketplace: sembrar la oferta a mano (10-15 causas semilla, fase 0). Canales priorizados: WhatsApp share > IG/TikTok orgánico > comunidades > prensa (narrativa post-Maratea, nota fácil) > micro-influencers > pauta (solo post-validación). Presupuesto $0 en fases 0-1, a propósito.
  - **Landing:** one-pager, un solo objetivo (descarga; pre-stores: mail). Hero con mockup 3D = **screenshots reales + frame + CSS perspective, explícitamente descartada la imagen de UI generada por IA** (mostraría una app que no existe, contradice la promesa de confianza). Headline propuesto "Ayudá sin dudar" (a testear). Estructura de 6 secciones con copy borrador. Dominio `donar.ar`/`dona.ar` a registrar YA. Segunda vida: página pública de causa `/c/[id]` (eslabón clave del loop, parqueada como 14.8).
  - **Financiación:** honestidad primero: donaciones con fee techo 5% NO es pitch VC clásico. Dos caminos no excluyentes: impacto/subsidios (mejor fit hoy: IDB Lab, Naves, Mayma, a validar) y pre-seed VC con la historia "infraestructura de confianza" (no "app de donaciones"). Recomendación: validar primero (fase 1), pitchear después. Elevator pitch de 30s redactado textual. Deck de 12 slides con contenido por slide (las diferenciales: slide 6 moat de confianza, slide 10 "el dato que nadie más tiene": producto completo construido por una persona AI-first con costo ~cero → el capital paga validación, no desarrollo). Tabla de objeciones con respuesta (curaduría no escala / GoFundMe / fee chico / regulación / fraude / por qué vos). Prerequisitos del pitch: opinión legal escrita + fee definido + números de H1/H2/H3.
- **Épica 14 nueva en `docs/BACKLOG.md`** con las tareas ejecutables que salen del doc: dominio (14.2, urgente), landing v1 (14.3), video-demo 60s (14.4), causas semilla (14.5), eje emocional a testear (14.6), ask a definir (14.7), página pública de causa (14.8, parqueada).
- **Decisiones que quedan en manos de Gastón:** eje emocional, monto del ask y camino (impacto vs. pre-seed), y cuándo disparar la consulta legal (ahora también bloquea el pitch, no solo la etapa 2).

### 29 jul 2026 (sesión 18, cont.: diseño del ícono/logo de la app, AI-first de punta a punta)
- **Terminado el ícono real de la app** (antes era el default de Expo). Es una mano abierta con un corazón héroe encima, sobre squircle celeste degradé. Todo el recorrido quedó documentado en `docs/proceso-logo.md` con los prompts exactos.
- **El método (la "máquina" de trabajo), que es lo que importa registrar:** (1) Claude explora conceptos en SVG y arma artifacts de vista previa para decidir visualmente; (2) se aprendió que las ilustraciones con volumen (manos) NO van en SVG a mano → se dividió el trabajo: **la IA de imagen (ChatGPT) genera UN master, Claude deriva los 8 assets localmente** con `sharp` + `opentype.js` (`scripts/generate-icons.mjs`); (3) el símbolo transparente para Android se **extrae del master por umbral de color** (más confiable que pedirle transparencia a la IA); (4) cada paso se valida con un artifact de preview antes de tocar el repo. Reproducible: la fuente es `assets/images/fuente-icono.png`, el resto se regenera con el script.
- **Decisiones de diseño clave** (todas en el doc): salir del corazón genérico de la categoría; diferenciarse sí o sí de Mercado Pago (sin óvalo ni contorno marino); aire + jerarquía (un solo héroe); herramienta según el asset. Sistema de identidad estilo Facebook: **solo símbolo en el ícono** (el SO pone "DonAR" debajo), **wordmark `donAR` dentro de la app** (login + header) y en el splash.
- **8 assets generados y enganchados en `app.json`** (icon 1024 sin alpha, Play 512, foreground/background/monochrome de Android, splash con wordmark, favicon). Splash y adaptiveIcon backgroundColor pasados a `#1E88E5`. Preview de registro: `docs/logo-preview.html`.
- **Feature graphic 1024×500 de Play HECHO** (`scripts/generate-feature-graphic.mjs`, por composición sin IA): degradé de marca + símbolo + wordmark `donAR` + tagline, lockup centrado. En `assets/images/store/`. Con esto están **todos los assets de marca para publicar**; solo quedan los screenshots de las fichas (13.2, se hacen con capturas reales + Figma). Ver `docs/proceso-logo.md` para el proceso completo.

### 29 jul 2026 (sesión 18, la más larga: Épicas 3.3-3.5, 4, 5, 6, 10 con Opus + Sonnet)
- **Arranque con Opus:** retomado el proyecto, revisado backlog completo y decidido plan de trabajo. Se cerraron TODAS las decisiones de producto que quedaban abiertas del 29 jul (sesión 17): timeout de confirmación (3.3, auto-confirmación a 48hs con countdown, patrón buyer-protection de Mercado Libre/AliExpress), cierre de causa por monto (4.1, reemplaza "meta = objetivo, no techo"), destino del excedente (4.4, resuelto: va al beneficiado sin custodia), agradecimiento (4.3, diseñado: mensaje de cierre + gracias puntual), Mercado Pago (oculto del todo, no "próximamente"). Todo esto se dejó escrito en `docs/BACKLOG.md` y en un plan de handoff nuevo, `docs/PLAN-SONNET.md`, para que la implementación (más barata) corriera con Sonnet.
- **Cambio de modelo a Sonnet, implementación de los 4 bloques del plan + extras:**
  - **Bloque A (Épica 4):** cierre de causa automático (client-side, sin cron), mini-reporte, mensaje de cierre + agradecimiento puntual (tabla nueva `cause_thanks`).
  - **Bloque B (3.3):** auto-confirmación a 48hs (`autoConfirmIfNeeded`, mismo patrón lazy que el cierre de causa) + countdown en los 3 lugares donde se ve una transferencia pendiente.
  - **Bloque C:** encontrado y arreglado un bug real en el ranking (`getMonthlyRanking` no filtraba `status`, sumaba puntos de aportes `pending`/`rejected`); kudos al donante (3.4, columna `confirmed_at`).
  - **Bloque D (Épica 10):** nombre y apellido reales en el registro, foto de perfil (bucket `avatars`), recuperar contraseña (deep link + pantalla nueva, código completo pero bloqueado por config de Resend/Supabase).
  - **Extra pedido en el momento:** 3.5 (lista de transferencias/aportes escalable, filas compactas + comprobante lazy), 5.4 (foto de perfil + mini-perfil de donante "estilo Airbnb" en el ranking, con monto donado incluido a pedido de Gastón), ajuste de 10.3 (Nombre/Apellido como dos campos en vez de uno, para tener las dos iniciales reales) y reubicación de "Cerrar sesión" al final del Perfil (benchmark Instagram/Uber/Airbnb).
- **Cada bloque se commiteó y pusheó a `main` por separado**, con `npx tsc --noEmit` y `npx expo lint` limpios antes de cada commit. Quedaron 3 migraciones SQL nuevas al final de `supabase/schema.sql` (cierre+agradecimiento, `confirmed_at`, `avatar_url`+bucket `avatars`) que Gastón tiene que correr en el SQL Editor si no lo hizo ya.
- **Interludio:** Expo Go no conectaba (misma WiFi, pero sin andar); se sugirió `-c` (caché limpia) y `--tunnel` como alternativas, sin confirmación de cuál lo resolvió.
- **Ronda final de la sesión, tres preguntas de producto + un bug reportado.** Gastón preguntó (sin pedir código todavía): ¿el admin debería poder eliminar causas?, ¿tiene sentido ver la propia causa en el feed (con benchmark "qué hace Instagram")?, ¿debería poder subirse video? Se revisó el código antes de opinar (no se supuso nada): confirmado que el curador HOY solo puede actuar sobre causas `review`/`needs_info` (`curar-screen.tsx`), nunca sobre una ya publicada → agujero real, anotado como **Épica 12 (moderación)** nueva en el backlog, recomendación de NO borrar de verdad (rompería trazabilidad) sino un estado `removed` nuevo. Video: anotado como **6.3**, con el llamado de atención de que GoFundMe tiene data real de que sube conversión, pero acotado a portada/historia (no a la evidencia del curador) y con las piezas nuevas que hacen falta (`expo-video`, límites de tamaño). La pregunta del feed SÍ se implementó ya (Gastón dijo "anotá todo salvo la 2, esa ajustala ahora"): confirmado en código que la causa propia se mezclaba con las demás en el feed general; se sacó (regla Instagram: el feed es para descubrir lo ajeno, lo propio vive en "Mis causas"), con cuidado del caso borde de estar probando en solitario (el estado vacío distingue "no hay causas" de "la única que hay es la tuya").
- **Bug encontrado y arreglado en el mismo paso:** Gastón reportó que Perfil "se recarga sola por unos instantes, es rarísimo". Diagnosticado en el código (no a ciegas): `profile.tsx` y `ranking.tsx` hacían `setLoading(true)` en CADA re-foco de la tab (no solo la primera vez), así que la pantalla entera se reemplazaba por un spinner de golpe cada vez que se volvía a esas tabs, aunque los datos ya estuvieran cargados. `activity.tsx` nunca tuvo este bug porque no reseteaba el loading en cada foco. Fix con un `useRef` (`hasLoadedOnce`) en ambas pantallas: el spinner de pantalla completa solo aparece la primera vez, los re-focos siguientes refrescan en silencio.

### 29 jul 2026 (sesión 17, cierre: password resuelta + "casi MVP" + ideas)
- **Contraseña de la cuenta vieja: RESUELTA.** El panel de Supabase solo ofrecía "send recovery" (mail que no llega, Resend sin dominio). Solución que funcionó: setear la pass directo en la base con SQL (`update auth.users set encrypted_password = crypt('elegida', gen_salt('bf')), email_confirmed_at = coalesce(email_confirmed_at, now()) where email = '...'`, requiere `pgcrypto`, viene por default en Supabase). Gastón entró con su cuenta real. **Método a reusar para cualquier cuenta vieja "ascendida desde anónima" sin contraseña.**
- **Gastón: "quedó hermoso el login, a eso me refería siempre".** El refactor a login obligatorio era exactamente lo que venía pidiendo. Confirmado que estamos **cerca del MVP**: "con una última pulida mañana ya estamos listos".
- **Tres ideas nuevas para backlog (NO se codearon, solo se ordenaron):** (1) **buscador + filtros + ordenamiento** del feed, con benchmark visual que mandó (app de delivery: barra "Buscar" + chips Filtrar/Ordenar/Descuentos/Destacados) → Épica 11, se solapa con la 7; (2) **recuperar contraseña** → sube prioridad en 10.2, depende de dominio propio en Resend; (3) **foto de perfil + iniciales reales** (pedir nombre/apellido en registro, subir avatar) → 10.3, "lo veo clave". 
- **Aplicada la regla de anclar en explosión de creatividad:** Gastón tiró tres ideas + un "estamos casi"; en vez de codear, se ordenaron en el backlog por épica y prioridad, se consolidó memory y se pusheó. Cierre de sesión limpio.

### 29 jul 2026 (sesión 16, LOGIN OBLIGATORIO: fin del modelo anónimo)
- **Contexto:** Gastón intentó probar con otro mail y "se rompió todo, imposible entrar". Diagnóstico: el código-por-mail nunca llegó a ese otro mail (Resend en modo prueba solo entrega al mail de su cuenta). Y su sesión activa se ensució en el intento, por eso tampoco veía la donación de su hermano.
- **Query confirmó que la donación está intacta:** "Me muero de hambre" tiene un aporte `$50.000 · status: pending · method: transfer · tiene_comprobante: true · owner_id: 744a56dd` (su cuenta real). No se perdió nada; solo faltaba estar logueado en esa cuenta para verlo/confirmarlo.
- **Decisión de Gastón (la venía pidiendo hace tiempo): LOGIN OBLIGATORIO, todo atado a una cuenta que persiste.** El modelo "anónimo por default + vincular opcional" era la raíz de TODOS los líos de identidad de las últimas sesiones (huérfanas, cruzadas, donaciones invisibles). Se implementó completo:
  - `lib/supabase.ts`: `ensureSession` ya no crea sesiones anónimas (devuelve `null` si no hay cuenta real, filtrando `is_anonymous`). Sacadas las 4 funciones de OTP/vincular (`linkEmail`, `confirmLinkEmail`, `loginWithEmail`, `confirmLogin`). Nuevas: `signUpWithPassword`, `signInWithPassword`, con `authError()` que traduce los errores de Supabase a español.
  - `causes-store.tsx`: `userId` viene de la sesión real; nuevo `loadSession` (setea userId + accountEmail + fetchCauses + isCurator, o limpia todo si no hay cuenta). `signOut` ya NO crea anónima, deja `userId=null` → vuelve al login. Nuevas acciones `signUp`/`signIn` (via helper `authenticate`), y `isAuthenticated`/`accountEmail` expuestos.
  - `screens/auth-screen.tsx` (NUEVO): pantalla de login/registro (mail + contraseña, toggle entrar/crear cuenta, validación 6+ chars, errores claros). Estilo donAR con el logo.
  - `_layout.tsx`: gate nuevo. Si `!isAuthenticated` (y no está cargando) → `AuthScreen`, antes que curador o tabs.
  - `profile.tsx`: sacado el componente `AccountLink` entero (vincular opcional, ya no aplica) y sus estilos muertos. El mail sale de `accountEmail` del store; siempre logueado.
- **Trade-off dicho en voz alta a Gastón:** pedir cuenta para donar baja conversión (GoFundMe deja donar de invitado). Aceptable porque la verificación es el eje de donAR. Se eligió password sobre código-por-mail porque password NO depende de Resend → se prueba YA con cualquier mail.
- **PASOS MANUALES PENDIENTES DE GASTÓN (documentados en `supabase/schema.sql` al final):** (1) apagar "Confirm email" en Supabase Auth (sin esto el registro no loguea); (2) su cuenta vieja (`gaston.product.expert@gmail.com`, ascendida desde anónima, sin contraseña) necesita una contraseña para entrar con el flujo nuevo: setear una via dashboard (reset password) o crear cuenta nueva y reasignar owner_id de sus causas. **Nada de esto se probó en dispositivo real.** `tsc` + `expo export` limpios.
- **Reset de contraseña ("olvidé mi contraseña"): NO implementado**, queda pendiente (necesita mail funcionando = dominio propio en Resend). Anotado en backlog.

### 29 jul 2026 (sesión 15, viewAsDonor: curador y donante sin cerrar sesión)
- **Gastón: "no veo lo que me donaron. tampoco sé en qué quedó el login".** Diagnóstico ordenado con preguntas puntuales (no asumir):
  1. Corrió el SQL de reasignación (o algo equivalente) — la query de diagnóstico ahora muestra "Me muero de hambre" con `owner_id` = cuenta real, `email: gaston.product.expert@gmail.com`, `is_anonymous: false`. **La reasignación de la causa quedó bien del lado de la base.**
  2. Pero su sesión ACTIVA en el celular sigue siendo anónima, sin vincular (`Perfil` muestra el cartel de vincular, no "✓ Vinculado"). Por eso no ve nada: la app filtra por la sesión actual, no por lo que diga la base.
  3. Revisado el código de "Vincular mail" (`AccountLink` en `profile.tsx`): YA es inteligente. Si el mail ya existe (como este caso), `linkEmail` falla con "already registered" y automáticamente cae a `loginWithEmail` (recupera la cuenta existente con un código, no crea una nueva). El pedido histórico de Gastón de "poder loguearse" ya está resuelto en el código, sin cambios necesarios ahí.
  4. **Problema real que Gastón detectó solo:** esa cuenta real (`744a56dd...`) es casi seguro la misma marcada `is_curator=true` en sesiones anteriores. Si se loguea ahí, `_layout.tsx` lo manda directo al panel de curador (pantalla completa, sin tabs), sin forma de ver Perfil/Recibiste — quedaría atrapado. Su duda ("a no ser que el panel pase a vivir en otro lado") era el diagnóstico correcto.
- **Fix implementado: `viewAsDonor`.** Nuevo estado en `causes-store.tsx` (`viewAsDonor` + `setViewAsDonor`, se resetea en `signOut`). `_layout.tsx`: la condición para mostrar `CurarScreen` pasa de `isCurator` a `isCurator && !viewAsDonor`. `curar-screen.tsx`: botón nuevo "Ver como donante" junto a "Cerrar sesión" (prende `viewAsDonor`, NO cierra sesión). `profile.tsx`: si `isCurator` es true (o sea, se está viendo como donante siendo curador), aparece "Volver a curar" junto a "Cerrar sesión" (apaga `viewAsDonor`). Misma cuenta, dos modos, sin perder la sesión en ningún momento. `tsc --noEmit` + `expo export --platform ios` limpios.
- **Plan de acción para Gastón:** en Perfil, tocar "Vincular mi mail" con `gaston.product.expert@gmail.com` → como ya existe, cae solo a login → código por mail → confirmar. Va a caer en el panel de curador por default (ahora que esa cuenta es curador); tocar "Ver como donante" para ir a Perfil → Recibiste y ver la donación de su hermano. **Nada de esto se probó en dispositivo real.**

### 29 jul 2026 (sesión 14, tab bar + Actividad real + bug orphaned identity)
- **`+` alineado horizontal + shrink-on-scroll: entregados** (pedido en la sesión anterior). `+` ahora es un botón inline en el centro de la pill (5 slots parejos), círculo brand 42px con ícono `add` de Ionicons. Shrink-on-scroll con `Animated` nativo de RN (no reanimated, sin `babel.config.js` en el repo no se puede confirmar el plugin de worklets); nuevo `tab-bar-scroll.tsx` (`TabBarScrollProvider`) conectado a feed/ranking/perfil/actividad.
- **SQL mal pasado, corregido.** Le di a Gastón el SQL envuelto en `Bash(cat <<'EOF'...)`, lo copió con el wrapper y le tiró error de sintaxis en Supabase. Aprendizaje: cuando el SQL es PARA COPIAR Y PEGAR en otro sistema (no para correr acá), va en texto plano en la respuesta, no a través de una tool de shell que el usuario pueda copiar por error.
- **Query confirmó la identidad huérfana:** "Me muero de hambre" (causa de Gastón) tiene `owner_id` anónimo, `email: null` — nunca se vinculó. Confirmada la teoría de la sesión anterior. Le di el SQL correcto en dos pasos (buscar su cuenta real con `is_anonymous = false`, reasignar `owner_id`). **Pendiente de que lo corra y confirme.** Ver detalle y la decisión de producto abierta (bloquear crear causa sin mail vinculado?) en `docs/BACKLOG.md`.
- **Gastón: "yo dije desde el principio que el usuario debería poder loguearse".** Aclarado sin ser defensivo: el login YA EXISTE (`loginWithEmail`/`confirmLogin` en `lib/supabase.ts`, pensado para recuperar cuenta desde otro dispositivo), pero no puede recuperar una identidad que nunca se vinculó a nada antes de perderse — no es que falte la función, es que el caso puntual (`signOut` sin vincular antes) queda fuera de lo que el login puede arreglar.
- **Barra de Android "se ve fatal" (captura del hermano): diagnosticada como probable bundle viejo en caché**, no reprodujo el diseño actual (pill redondeada, márgenes) sino el diseño full-width viejo. Pedido a Gastón: recargar la app entera en el Android del hermano, no solo esperar Fast Refresh, para confirmar. Aparte, encontrado y arreglado un bug real independiente de la teoría: el `elevation` (sombra) de Android estaba en el contenedor exterior (`pill`), que no tenía su propio `borderRadius` (solo la capa interior `glass` lo tenía) — trampa conocida de Android, la sombra puede salir cuadrada. Agregado `borderRadius` también en `pill`.
- **Push notifications: investigado y BLOQUEADO en Expo Go.** Confirmado por WebSearch: desde el SDK 53 de Expo, las notificaciones push remotas no funcionan en Expo Go (ni iOS ni Android), se sacaron por abuso de cuota de FCM. Este proyecto está en SDK 54 y TODO el testing es por Expo Go/túnel. Un push real necesita un dev build (EAS), ya parqueado para "antes de subir a las stores". Documentado como Épica 9.2 (decisión abierta: ¿adelantar el dev build solo para esto?).
- **Construido en su lugar: tab Actividad con contenido real** (ya no el placeholder "Próximamente"), como la pieza que SÍ se puede hacer hoy sin push. Tres secciones: "Para revisar" (transferencias pendientes en mis causas, `getPendingTransfersForMyCauses`, función nueva del store), "Te llegaron" (aportes confirmados recibidos, `getReceivedContributions`, función nueva), "Tus aportes" (reusa `getMyActivity().contributions`). Cada fila lleva al detalle de la causa correspondiente. Conectada al shrink-on-scroll.
- **Perfil: "Donaste"/"Recibiste" tapeables** (pedido de Gastón). Dos pantallas nuevas en el Stack, `donated.tsx` y `received.tsx`, mismo look que el resto (header con `‹` + lista). El bloque "Historial de impacto" que antes vivía siempre visible en el Perfil se sacó de ahí y ahora es el contenido de `/donated` (perfil queda más corto). `/received` es contenido 100% nuevo (antes solo existía el TOTAL `receivedTotal`, no el detalle por aporte).
- **Loaders revisados** (pedido de Gastón, "si es que lo amerita"). `review.tsx` (publicar causa) ya tenía spinner completo, sin cambios. `transfer/[id].tsx` (el único camino de donar vivo hoy, MP apagado) solo cambiaba texto a "Enviando..."; sumado `ActivityIndicator` + "Subiendo comprobante...", mismo patrón que `review.tsx`. Se evaluó y se decidió NO tocar el botón de MP (deshabilitado con flag, cero valor agregar loader a una feature apagada).
- **Volumen de la sesión:** pedido con 3 features grandes + 1 bug urgente + 1 reclamo de producto + 1 diagnóstico de UI en simultáneo ("explosión de creatividad" con trabajo concreto pedido, no solo ideas sueltas). Se usó `TaskCreate`/`TaskUpdate` para no perder el hilo (6 tareas, todas completadas). `tsc --noEmit` y `expo export --platform ios` limpios al final. **Nada de esto se probó en dispositivo real** — pendiente de Gastón: probar Actividad, el historial donado/recibido, el `+`/shrink de la pill, y confirmar si la barra de Android sigue viéndose mal tras recargar del todo.

### 29 jul 2026 (sesión 13, bug crash Android por blur + feedback pill)
- **Bug urgente (hermano de Gastón, Android por túnel): pantalla de estado de la causa EN BLANCO TOTAL** al abrirla desde el feed. Diagnóstico ordenado (aplicando la regla de paridad y de preguntar el dispositivo): confirmado Android + túnel (= tiene la barra de vidrio nueva) + blanco total sin header (= la pantalla no se monta, la app crashea) + funciona en el iPhone de Gastón. Causa raíz: **`experimentalBlurMethod="dimezisBlurView"` de `expo-blur` crashea en Android durante las transiciones de pantalla** (al pushear cause/[id] desde el feed, con la tab bar de blur animándose, la app muere a blanco). En iOS no pasa (blur nativo estable). Fix: en Android NO se usa `BlurView`, se usa un tinte sólido translúcido (`rgba(248,251,253,0.94)`) vía `Platform.OS`, estable y visualmente coherente con la pill; iOS mantiene el blur real. El blur real de Android se retoma con una vía estable en el build nativo. **OJO red herring:** el error que Gastón vio en la terminal (`Failed to resolve the Android SDK path` / `spawn adb ENOENT`) NO era el bug: es solo que su Mac no tiene Android SDK instalado, inofensivo (el hermano usa Expo Go por QR, no necesita adb). El crash real era nativo (no salía como error JS en Metro). `tsc` limpio. Como el hermano está por TÚNEL, el fix llega solo por Fast Refresh (sin push/pull); solo tiene que recargar la app.
- **Feedback de Gastón sobre la pill (a aplicar después del bug):** (1) le gustó la versión nueva; (2) el `+` "quedó flotando", lo quiere **alineado horizontal** con los íconos (no sobresaliendo) → cambiar el diseño del `+`; (3) quiere avanzar con el **shrink-on-scroll** (backlog 8.2). Observación suya: le llama la atención que apps famosas (Twitter, FB, Threads, YouTube) no tengan la barra flotante así.
- **Ambos aplicados (pendiente de OK visual de Gastón):**
  - **(1) `+` alineado horizontal:** ahora es un botón inline en el centro de la pill (5 slots parejos: 4 íconos + el `+`), círculo brand de 42px con el ícono `add` de Ionicons, destacado por color pero SIN sobresalir ni `marginTop` negativo. Se simplificó la estructura (ya no hace falta el truco de dos capas para que el `+` no se recorte, porque ya no sobresale; igual se mantiene la capa `glass` separada para redondear el vidrio).
  - **(2) shrink-on-scroll:** con el **`Animated` nativo de RN, NO reanimated** (decisión consciente: el proyecto NO tiene `babel.config.js`, así que no se puede confirmar que el plugin de worklets de reanimated esté activo; el Animated nativo no lo necesita y con `useNativeDriver` corre igual en el hilo nativo). Nuevo componente `src/components/tab-bar-scroll.tsx`: `TabBarScrollProvider` (envuelve el `<Tabs>` en `(tabs)/_layout.tsx`) expone un `Animated.Value` (0=expandida, 1=achicada) + un handler `onScroll`. El handler detecta dirección (deslizar abajo → achica, arriba o tope → expande, con umbral de 6px). La tab bar consume el valor y anima el wrap (`translateY 0→20`, `scale 1→0.86`, `opacity 1→0.92`). Conectado el `onScroll` (+`scrollEventThrottle={16}`) en los 3 ScrollView con scroll: feed, ranking, perfil (activity es placeholder sin scroll). `tsc` + `expo export` limpios. **NO pusheado: espera OK visual de Gastón por túnel.** Knobs: umbral (6px), cuánto achica (scale 0.86), cuánto baja (20px), duración (200ms).

### 29 jul 2026 (sesión 12 cont., tab bar de vidrio + prototipo liquid glass)
- **Pregunta de Gastón: ¿se puede una tab bar estilo "liquid glass" como Instagram/iOS 26?** Respondida con los trade-offs (ver también nueva entrada de backlog): hay dos cosas distintas. (1) El **Liquid Glass nativo real** de iOS 26 (refracción + brillos, vía `expo-glass-effect`/`GlassView`): es iOS 26+ ONLY (Android nunca lo tiene nativo → rompe paridad) y NO corre en Expo Go (necesita build nativo, que está parqueado para antes de las stores). (2) La **aproximación frosted glass con `expo-blur`**: corre en Expo Go, funciona iOS + Android, da ~80% del efecto sin la refracción. Decisión acordada: hacer AHORA la versión blur (paritaria, en Expo Go) y dejar el Liquid Glass nativo real para el build nativo, con el blur como fallback de Android. Un diseño, dos calidades según plataforma.
- **Primer prototipo (barra full-width de vidrio): descartado por feedback de Gastón.** Mostró capturas de la tab bar nueva de Instagram (pill flotante oscura, redondeada, íconos vectoriales finos, se achica al scrollear) y dijo que la barra full-width con emojis "parece de mala calidad". Pidió: pill flotante redondeada + íconos menos "fuertes" (no emojis) + shrink-on-scroll.
- **Segundo prototipo (pill flotante, el que quedó, pendiente de OK visual):** decisiones tomadas con Gastón vía AskUserQuestion: (1) **vidrio claro** que "sigue el fondo" (por ahora la app es clara; dark/light según sistema → backlog 8.5); (2) **`+` sobresaliente** (mantiene protagonismo de crear causa, no integrado en la pill). `donar-tab-bar.tsx` reescrito como pill: `wrap` absolute con `paddingHorizontal:16` (márgenes laterales) y `paddingBottom` safe-area (despegada del borde), pill de height 62 / radius 30 con sombra suave para "flotar". Estructura en DOS capas para redondear el vidrio sin recortar el `+`: capa `glass` (absoluteFill, `overflow:hidden`, borderRadius, contiene el `BlurView` intensity 30 tint light + tinte `rgba(255,255,255,0.58)`) y capa `row` encima (íconos + `+`, sin recorte, el `+` sobresale con `marginTop:-30`). Íconos: **Ionicons** (`@expo/vector-icons`, ya venía con Expo) outline cuando inactivo / filled cuando activo (patrón Instagram), activo con fondo sutil `rgba(74,144,226,0.14)`; sin labels de texto (más limpio). El `+` con borde blanco de 3px para separarlo del vidrio. Paridad: `experimentalBlurMethod="dimezisBlurView"`. `tsc` + `expo export` limpios (1140 módulos, +74 de vector-icons). **NO pusheado: espera OK visual de Gastón en su iPhone.** Knobs para ajustar en persona: intensidad blur (30), tinte (0.58), radio (30), altura (62), cuánto sobresale el `+`. Próximos pasos si le gusta: shrink-on-scroll (8.2, con reanimated ya instalado), vidrio en el header (8.3), theme sistema (8.5).

### 28 jul 2026 (sesión 12, README + fix CTA Android + fix curador)
- **README actualizado** al estado real: estado de rebanadas, estructura del repo y flujo de trabajo (git directo a `main` es intencional, corregido texto que decía lo contrario).
- **Backlog listado con esfuerzo/impacto** a pedido de Gastón, para priorizar próximos pasos.
- **Bug reportado por Gastón** (captura): en Android, el botón "Donar a esta causa" queda tapado por la barra de navegación del sistema (3 botones); en iPhone 14 Pro se ve bien. **Diagnosticado y resuelto en la misma sesión:** todas las pantallas usan `SafeAreaView edges={['top']}`, así que el CTA fijo al pie nunca descontaba el inset inferior (en iOS la gesture bar es más angosta y no se notaba). Fix aplicado en los 8 lugares con footer/CTA fijo: `cause/[id].tsx`, `donate/[id].tsx`, `transfer/[id].tsx`, `create.tsx`, `cobro.tsx`, `review.tsx`, `gracias.tsx`, `curar-screen.tsx` — mismo patrón que ya usaba `donar-tab-bar.tsx` (`paddingBottom: Math.max(insets.bottom, Spacing.lg)`). `npx tsc --noEmit` limpio. **Pendiente de Gastón: probar en su Android real** que ahora se vea bien (no se pudo verificar visualmente en esta sesión, solo el fix de código + type-check). Pusheado a GitHub.
- **Bug del curador que no ve causas en revisión (backlog, 28 jul): resuelto.** Causa raíz real: la RLS del lado de Supabase ya andaba bien (evalúa `is_curator` en vivo contra la DB en cada query), el problema era 100% client-side: `causes-store.tsx` carga `causes` una sola vez al montar y nunca lo releía. Dos fixes: (a) `causes-store.tsx` ahora refetchea automáticamente si `isCurator` pasa a `true` dentro de la misma sesión; (b) `curar-screen.tsx` suma pull-to-refresh (`RefreshControl`) en la lista de causas pendientes, para el caso más probable (llega una causa nueva mientras el panel ya está abierto, sin necesidad de reabrir la app). `npx tsc --noEmit` limpio. **Pendiente de Gastón: probar el pull-to-refresh en el panel `/curar` real.**
- **Bug del swipe nativo para volver atrás (backlog, 27 jul): resuelto, con foco explícito en paridad.** Causa: las pantallas del flujo (create/cobro/review/donate/transfer/cause) eran `Tabs.Screen` con `href:null`, no un stack real, así que no había navegación nativa en NINGUNA plataforma (ni swipe-back en iOS ni retroceso correcto en Android; el reporte original solo mencionaba iOS, pero el problema era estructural y afectaba a las dos). Fix arquitectónico, no un parche: los tabs (`index`, `ranking`, `profile`, `activity`, `explore`) se movieron a un grupo de rutas `(tabs)/` con su propio `Tabs`; `_layout.tsx` raíz pasó a ser un `Stack` real, con `(tabs)` como una pantalla y el resto del flujo empujándose encima (esto da swipe-back nativo en iOS y back-button/gesto de Android automáticamente, gratis, por venir de `@react-navigation/native-stack`). Limpieza de paso: sacado el `HIDDEN_ON` manual de `donar-tab-bar.tsx` (quedó redundante, la estructura ya garantiza que el tab bar no se vea en esas pantallas). Verificado con `npx tsc --noEmit` y `npx expo export --platform ios` (bundle limpio, sin correr en dispositivo real). **Pendiente de Gastón: probar el swipe-back en iPhone Y confirmar que el botón de retroceso/gesto de Android también navega bien para atrás.**
  - **Revisión con Opus (pedido de Gastón: "que lo gestual y la navegación se sientan SUPER smooth y bien Apple, como amaría Steve Jobs").** El cambio a Stack estaba bien de estructura pero el código de navegación seguía escrito para cuando todo era Tabs planas, lo que producía incoherencias que se notan en el feel. Corregido, todo con foco en paridad iOS/Android: (1) los botones "‹" de `cobro` y `review` usaban `router.replace(pantalla_anterior)` → cambiados a `router.back()`; con replace el gesto de swipe (pop, sale a la derecha) y el botón (replace, anima como push entrando de la derecha) hacían animaciones OPUESTAS para la misma acción de volver. (2) `review` al crear con éxito hacía `router.replace('/cause/id')`, dejando el flujo de creación vivo debajo (swipe-back caía en "cobro") → ahora `router.dismissAll()` + `router.push`, así el swipe-back va al feed limpio; extraído en `goToCreatedCause`, usado también en el `onRequestClose` del modal (antes vacío, back de Android muerto). (3) `gracias` ahora es TERMINAL: `gestureEnabled:false` en el Stack (bloquea swipe-back de iOS a la donación ya cerrada) + `BackHandler` que intercepta el back físico de Android (paridad) + sus botones hacen `dismissAll()` en vez de `replace` (no dejan el flujo debajo). (4) dos saltos entre tabs (`ranking`→`profile`, `profile`→feed) usaban `router.push` (apila el tab sin tab bar) → cambiados a `router.navigate` (cambia de tab de verdad). Mapa final coherente: back = pop nativo siempre, forward = push, terminal = replace/dismiss, entre-tabs = navigate. Decisión consciente: NO habilitar `fullScreenGestureEnabled` (swipe desde cualquier parte) porque pelearía con el gesto horizontal de los carruseles; el edge-swipe default de iOS es lo correcto acá. `tsc` + `expo export` limpios.
- **Bug del carrusel no swipeable desde el feed (backlog, 27 jul): resuelto, tras dos intentos.** Primer intento (`nestedScrollEnabled`, prop de Android) fue incorrecto: Gastón aclaró que él vio el bug en SU IPHONE 14 PRO, no en Android, lo que descartaba esa causa (prop no-op en iOS). Reconsiderado con ese dato: la hipótesis ORIGINAL del backlog (el `Pressable` de toda la tarjeta compitiendo con el `ScrollView` horizontal) era la correcta, multiplataforma. Confirmado por contraste: el carrusel de `cause/[id].tsx`, que NO tiene un `Pressable` ancestro (solo vive dentro del scroll vertical de la pantalla), nunca se reportó roto. Fix real en `cause-card.tsx`: con fotos, la portada es un `View` simple (no `Pressable`); el tap para abrir el detalle vive en cada foto individual (`Pressable` hijo del `ScrollView`, patrón estándar que sí convive con el scroll) y en el bloque de texto de abajo (`Pressable` aparte). Sin fotos, la portada entera sigue siendo un solo `Pressable` (sin `ScrollView`, sin conflicto). `npx tsc --noEmit` limpio. **Aprendizaje para futuras sesiones: preguntar el dispositivo de testing ANTES de diagnosticar un bug de gestos/UI, no asumir.** **Pendiente de Gastón: probar el swipe del carrusel en el feed, en su iPhone.**

### 28 jul 2026 (sesión 11, testing P2P + cierre)
- **MVP de pago probado en flujo real:** otra persona corrió la app en Expo Go (Android, otra red, vía `npx expo start --tunnel`) y cargó una causa → quedó en revisión. Gastón probó desde su iPhone.
- **Bug/fricción encontrada 1 — el curador no ve la causa en revisión.** Dos causas: (a) vincular mail NO te hace curador (es un flag `is_curator` aparte, se prende con SQL puntual); (b) aunque seas curador, la app carga las causas al montar y NO las re-lee cuando pasás a curador, así que las causas en revisión de otros no aparecen hasta **reabrir la app**. MEJORA PENDIENTE (en backlog): refrescar causas automáticamente al confirmarse `isCurator`.
- **Bug/fricción encontrada 2 — re-login "No pudimos enviar el código".** Es rate limiting del OTP: Resend/Supabase tienen mínimo ~60s entre mails + tope por hora. Vincular → cerrar sesión → reintentar rápido lo dispara. Solución: esperar 2-3 min y pedir el código una sola vez. Es fricción esperada del setup OTP+SMTP, no un bug de la cuenta.
- **Cómo probar en Android:** mismo `npx expo start` (o `--tunnel` si está en otra red); en Android se escanea el QR **desde adentro de Expo Go** ("Scan QR code"), no con la cámara del sistema (eso es iOS). Los dos teléfonos pueden correr a la vez contra el mismo server.
- **Pendiente de Gastón:** correr la migración de `platform_support` (para el auto-reporte "ya aporté"); confirmar `is_curator` en su perfil vía SQL si quiere curar.
- **Nota de eficiencia:** la conversación se hizo muy larga. Regla acordada: un hilo por tanda; al retomar, sesión nueva + "leé memory.md y BACKLOG.md". La doc al día es lo que lo hace barato.

### 27 jul 2026 (sesión 10, madrugada tardía)
- **Informe del fundador** (artifact visual): a pedido de Gastón, con data real de git (cadencia, night-owl 61% después de medianoche), scorecard vs promedio (cadencia 9, profesionalismo 9, seniority producto 8, expertise 7.5, originalidad 7), análisis de originalidad y de monetización. URL: https://claude.ai/code/artifact/c29f0199-c6df-40cc-98bd-f382be5d75bf
- **Explicado lo legal en criollo** (a Gastón le cuesta lo legal) e **investigadas las fuentes reales** (BCRA PSPCP, CNV crowdfunding, UIF). Hallazgo central: el régimen se dispara por TENER plata, no por conectar. Ver decisiones y paper 11.c.
- **Pregunta de negocio de Gastón: "¿cómo cobro la comisión si la plata no pasa por mí?"** → resuelto: el SPLIT DE PAGOS de la pasarela le manda a donAR solo su comisión propia; recibir comisión propia ≠ custodiar. Para transferencia pura, aporte voluntario del donante (GoFundMe).
- **IDEA de Gastón para el MVP (muy buena):** apagar MP, dejar solo transferencia + un paso de "aporte voluntario a donAR" (modelo GoFundMe). IMPLEMENTADO: `MP_ENABLED=false` (flag, no borrado) en `donate/[id]`; transferencia es el único método; pantalla post-transferencia con card "Bancá el proyecto" (alias de donAR copiable + auto-reporte "ya aporté" a tabla `platform_support`). TODO: poner el alias real de donAR en `gracias.tsx` (hoy placeholder `donar.proyecto`).
- **Documentación legal sumada donde corresponde** (Gastón dijo "es clave"): paper sección 11.c nueva, README sección "Encuadre legal", memory (decisiones), backlog.

### 27 jul 2026 (sesión 9, madrugada)
- **Ranking mensual construido** (fórmula núcleo del paper, solo donantes con mail vinculado). Ver avance de rebanadas.
- **Mercado Pago sandbox andando.** Edge Function + client + wiring. Gastón la deployó desde el dashboard (el primer intento falló al bundlear por el import de deno.land; se reescribió con `Deno.serve` nativo y borrando el template default del editor). Sacó el token de PRUEBA, creó un usuario de prueba comprador para pagar (loguearse con la cuenta real daba "una de las partes es de prueba"). CLAVE que entendió Gastón: el demo cobra a UNA cuenta (custodial), no al beneficiado; el alias declarado no está conectado al pago.
- **Sesión de producto grande** (Gastón en "explosión de creatividad", pidió explícitamente que yo ancle y mantenga el orden/eje). Se definió:
  - Dos ejes que estaba mezclando: (A) panel de "mi causa" del creador, (B) métodos de pago del donante.
  - Dos métodos de pago: MP + transferencia con comprobante. La transferencia es el flujo canónico del MVP (no-custodial, el del paper); MP como está es demo custodial.
  - Modelo de confianza del dinero sin custodia (la parte que Gastón llamó "oro" y quiso documentar): transparencia + confirmación del beneficiado + identidad; solo lo confirmado suma. Respondida su pregunta de "cómo me fío del beneficiado" con el análisis de incentivos. Documentado en el paper 11.b.
  - Flujo nuevo que sumó: cierre de causa (por tiempo o meta) + mini-reporte + agradecimiento del beneficiado a los donantes.
  - Regla nueva: monto mínimo para pedir.
- **Creado `docs/BACKLOG.md`** (backlog vivo por épicas) y **actualizado el paper** (sección 11.b nueva + fila de fraude). Pedido explícito de Gastón: actualizar memory de forma RECURRENTE y mantener la doc de negocio al día (registrado en sección 2).
- Bug reportado: la foto de portada no se ve → DIAGNOSTICADO Y RESUELTO en la misma sesión: el view `causes_public` con `select c.*` no exponía columnas agregadas después. Fix por recreación del view (Gastón corre el SQL).
- Priorización: Gastón eligió arrancar por lo independiente (no depende de decisiones abiertas). Se hizo: (1) fix del bug de la foto, (2) Épica 1 (panel de mi causa). PRÓXIMO sugerido: Épica 2 (dos métodos de pago: MP + transferencia), que devuelve el eje "conectar sin custodiar".

### 26 jul 2026 (sesión 8, noche)
- **Pulido a fondo del flujo de crear causa**, todo por feedback de Gastón probando en su iPhone:
  - Datepicker roto (renderizaba una barra gris vacía): causa = `display="spinner"` dentro de un `Modal` con la nueva arquitectura de RN. Fix: la fecha se elige SOLO tocando el campo (se sacó el input de texto libre), iOS usa `display="inline"` (calendario), Android su diálogo nativo. Bonus: `minimumDate` estaba inline (`new Date()`) y se recalculaba en cada render corriendo el mínimo mientras se giraba la rueda; ahora fijo al montar con `useState(() => startOfToday())`.
  - Validación de fecha: al ser solo-picker con mínimo=hoy es imposible ingresar una inválida (22/22 o pasada). El gate de abajo lista "Falta: una fecha de cierre válida".
  - Montos pre-seteados se mezclaban con la fecha: separados en filas independientes (Monto+chips / Fecha), a sugerencia de Gastón. Chips de monto ($100k/$500k/$1M/$3M) que rellenan pero dejan editar.
  - Cobro: sacado el selector Mercado Pago/CBU (era redundante, mismo campo de texto). Ahora un solo input "Alias o CBU"; el método se infiere (regex 22 dígitos = cbu, si no = mp) para que curador/review sigan mostrando la etiqueta correcta.
  - "Crash" al volver atrás en la confirmación: NO era crash, `router.back()` caía al tab activo (create/cobro/review son pantallas hermanas del mismo Tab, no una pila). Fix: cada "volver" apunta al paso anterior explícito (`router.replace('/create')` etc). Como todo cuelga del mismo `draft`, editar ya funcionó sin más.
  - Fotos de portada (feedback: "estaría bueno subir 1-2 y verlas como carrusel"): implementado. Hasta 2, opcionales, bucket público `cause-covers`, columna `image_urls text[]` (reemplazó `image_url`). Carrusel horizontal con `ScrollView pagingEnabled` + puntitos en `cause-card` y `cause/[id]`; fallback emoji+color. Subida con `uploadCoverPhoto` (fetch+arrayBuffer, mismo patrón que la evidencia). Seed actualizado. `expo-file-system`/`base64-arraybuffer` ya se habían sacado en la sesión anterior.
  - Historia con IA: Gastón preguntó por pulir la historia con IA. Se le explicó (con la skill claude-api cargada para no inventar números): costo por uso casi nulo, pero necesita Edge Function + su cuenta Anthropic. DECIDIÓ posponerlo (ver Próximos pasos #7).
- **Aprendizaje de infra Supabase (Resend, del feedback previo):** Supabase ya no deja editar plantillas de mail sin un SMTP propio configurado. Por eso hubo que dar de alta Resend (gratis, sin tarjeta) para que el código de 6 dígitos (`{{ .Token }}`) funcione. El sender de prueba `onboarding@resend.dev` probablemente solo entrega al mail de la cuenta de Resend.
- **Patrón de migraciones idempotentes:** una migración con `drop column ... / update ... array[col_vieja]` falla si ya se corrió antes (la columna vieja ya no existe). Aprendizaje: al pasarle una migración a Gastón para re-correr, hacerla idempotente (`add if not exists`, `drop if exists`, `drop policy if exists` antes de `create policy`, `on conflict do nothing` en buckets). Pasó con `cause-covers`.

### 26 jul 2026 (sesión 7, mañana/tarde)
- Priorización con matriz esfuerzo/impacto de los pendientes del backlog; Gastón eligió avanzar con identidad cross-device (#3), limpieza de código muerto (#5) y curaduría real (#6), en ese orden.
- **#5 limpieza:** eliminado todo el scaffold del template de Expo sin uso real (`app-tabs`, `app-tabs.web`, `themed-text/view`, `hint-row`, `web-badge`, `ui/collapsible`, `animated-icon` + variantes, `hooks/use-theme`, `hooks/use-color-scheme` + variantes, `constants/theme.ts`), confirmado que nada en `src/app/` los importaba. `data/causes.ts` (mock) reemplazado por solo el tipo `Cause`, ya no se usa como datos. `npx tsc --noEmit` pasó de ~15 errores a limpio.
- **#3 identidad cross-device:** implementado vincular/recuperar cuenta por mail con código de 6 dígitos (no magic link, por la limitación de deep linking en Expo Go). Nuevas funciones en `src/lib/supabase.ts`: `linkEmail`, `confirmLinkEmail`, `loginWithEmail`, `confirmLogin`. UI en `profile.tsx` (componente `AccountLink`). Requirió configurar SMTP custom (Resend, gratis) porque Supabase no deja editar plantillas de mail (para exponer `{{ .Token }}`) sin uno propio. Gastón vinculó su mail real y quedó operativo.
- **#6 curaduría real:** la causa ya no se auto-aprueba. Nuevo esquema: `profiles.is_curator`, `causes.review_note`/`reviewed_at`, estados `needs_info`/`rejected` sumados al enum, tabla `cause_evidence` (DNI frente/dorso, selfie, documento de respaldo) y bucket privado de Storage `cause-evidence`, todo con RLS (dueño + curador). `create.tsx` pide las 4 fotos con `expo-image-picker`. Nueva pantalla de curador: cola de causas pendientes, ve la evidencia con URLs firmadas, botones Aprobar/Rechazar/Pedir info. El creador ve el estado en el feed ("Tus causas") y en el detalle de su causa, con el motivo si corresponde y botón "Reenviar a revisión" para `needs_info` (reenvía tal cual, sin edición, deuda documentada). Gastón se marcó a sí mismo como curador vía SQL puntual después de vincular su mail.
- **Seed de prueba:** creado `scripts/seed-demo-cause.mjs`, siembra una causa a nombre de una sesión anónima nueva (no la de quien lo corre), para poder donar sin que cuente como "recibido" propio. Corrido una vez en la base real.
- **QA de Gastón sobre lo construido**, todo resuelto en la misma sesión:
  - Header del modal de evidencia en `/curar` tapado por el Dynamic Island del iPhone 14 Pro → `SafeAreaView` no calcula bien el inset dentro de un `Modal` de React Native; arreglado con `useSafeAreaInsets()` explícito en el header del modal.
  - Subida de evidencia muy lenta → las 4 fotos subían secuencialmente; cambiado a `Promise.all` (paralelo). Sumado spinner real ("Subiendo evidencia...") en vez de solo texto.
  - Botón de "Enviar a revisión" se quedaba tildado en un segundo intento → mismo bug de clase que ya había pasado con el botón de donar (pantallas de Tab no se desmontan, `submitting` quedaba en `true` después de un envío exitoso). Arreglado con `useFocusEffect` reseteando el estado al enfocar. **Patrón a recordar:** cualquier estado local de "submitting/loading" en una pantalla que vive en un Tab necesita resetearse en focus, no solo en el catch de error.
  - Pantalla de cobro (`cobro.tsx`) prometía "Vincular Mercado Pago" pero era el mismo campo de texto libre que CBU, sin conexión real → renombrado a "Alias de Mercado Pago" / "CBU o alias bancario", copy honesto sobre lo que realmente pasa (no se construyó integración real, fuera de alcance).
  - Confirmado (sin cambios, ya andaba bien): una causa `en revisión` nunca llega al feed de otros usuarios, ni siquiera a nivel de los permisos de la base (RLS); solo la ve quien la creó, en su propia sección "Tus causas".
- **Separar curador y donante** (a pedido de Gastón, misma sesión): mezclar ambos roles en la misma navegación "quedaba una ensalada" para probar. Rediseño de `_layout.tsx`: decide una sola vez, antes de montar nada, si la cuenta es curador (`isCurator` del store) y en ese caso renderiza directo el panel de curaduría, sin tabs. El panel se movió de `src/app/curar.tsx` a `src/screens/curar-screen.tsx` (ya no es una ruta navegable, es un modo completo de la app; por eso también perdió su dependencia de `useRouter`/`useFocusEffect`, que rompía al no vivir dentro de un navigator). `signOut` nuevo en el store cierra sesión y arranca una anónima limpia, para poder alternar "probar como curador" / "probar como donante" en el mismo celular. Sacado el botón "Panel de curador" de Perfil (dead code, un curador nunca llega ahí); agregado "Cerrar sesión" junto al mail vinculado.
- **Bug: "Enviar a revisión" no hacía nada.** Causa raíz: la subida de evidencia usaba la API `File` de `expo-file-system` (nueva en el SDK 54), que podía fallar sin ningún error visible; si fallaba dentro del `Promise.all`, `publishDraft` quedaba sin resolver de forma clara y el botón parecía muerto. Arreglado reescribiendo `uploadEvidence` con `fetch(uri).arrayBuffer()` (patrón mucho más probado para RN + Supabase Storage), envuelto en try/catch, y sacadas las dependencias `expo-file-system`/`base64-arraybuffer`. De paso: `review.tsx` con try/catch/finally + `Alert` si falla (antes fallaba en silencio); `create.tsx` ahora lista qué falta ("Falta: DNI dorso, Selfie...") en vez de solo deshabilitar el botón sin explicar, y avisa si el permiso de fotos está denegado.
- **Modal de confirmación al enviar una causa**, a pedido de Gastón ("si no queda una sensación vacía"): en vez de navegar en silencio al feed tras publicar, `review.tsx` muestra un modal con felicitación, una frase al azar, el SLA (24 hs hábiles) y un botón que lleva directo al detalle de la causa recién creada (que ya muestra el estado "en revisión"). De paso, corregida una inconsistencia: el texto de la pantalla decía "menos de 48 hs", el SLA documentado es 24 hs hábiles.

### 25 jul 2026 (sesión 6)
- Primera charla de validación del proyecto: no es una pavada, hay demanda real (precedente Maratea), pero el supuesto regulatorio sigue siendo el que más puede cambiar el modelo.
- Backlog priorizado con matriz esfuerzo (tokens estimados) x impacto (valor para el objetivo de hoy: flujo de donación + visualización de un donante).
- Bug de QA de Gastón: todas las causas eran propias, entonces donar quedaba igual a recibido, imposible de testear la distinción. Resuelto: sumado soporte de imagen ilustrativa (`image_url`, con fallback a emoji+color) y creado `scripts/seed-demo-cause.mjs` para sembrar una causa ajena.
- Confirmado que el detalle de causa ya mostraba trazabilidad (aportes recientes) correctamente, sin cambios necesarios.
- Placeholder de Ranking/Actividad: texto cambiado de "Próxima rebanada" a "Próximamente" (a pedido de Gastón). Actividad se deja TBD, valor post-MVP.
- `supabase/schema.sql` reorganizado con una sección de migraciones al final, fecha por fecha, para no tener que repegar el archivo entero cada vez (patrón que se repitió en la sesión siguiente).

### 24 jul 2026 (sesión 5)
- Corregido `AGENTS.md`: punto de entrada para Claude Code / Cowork, apunta a memory.md, fija SDK 54, reglas de trabajo de Gastón, git directo a main OK.
- Creada skill de estilo en el repo: `.claude/skills/estilo-gaston/SKILL.md`.
- Continuidad Cowork -> Claude Code: el chat no se transfiere, pero todo lo importante vive en el repo (memory.md, docs/, AGENTS.md, skill de estilo).

### 24 jul 2026 (sesión 4)
- DECISIÓN de producto: la meta de una causa es un OBJETIVO, no un techo. Se puede donar aunque la causa ya llegó o supere la meta, con aviso al donante. Causas SIN monto = descartadas para el MVP.
- Implementado en `donate/[id].tsx`: aviso inline + confirmación al superar la meta.
- PENDIENTE (legal + trazabilidad): destino del excedente cuando una causa supera su meta. Hasta definirlo, el aviso es neutro.

### 24 jul 2026 (sesión 3)
- Perfil: medallas tapeables con modal (estado + frase de altruismo al azar).
- Donar: agregado monto personalizado.
- BUG RESUELTO "no se podía volver a donar": pantallas de Tabs no se desmontan, `submitting=true` quedaba para siempre. Fix con `useFocusEffect`. Este mismo patrón de bug volvió a aparecer el 26 jul en `review.tsx`; queda como algo a revisar por default en cualquier pantalla nueva con estado de envío.

### 24 jul 2026 (sesión 2, tarde)
- Construida pantalla de Perfil: donado + recibido, medallas y nivel, derivado de datos reales de Supabase.
- Agregado `getMyActivity()` al store.

### 24 jul 2026 (sesión 1)
- Retomado el proyecto. Revisados paper fundacional y manual de trabajo.
- Creado este memory.md.
- Diseñado el flujo de verificación de causas v0.1 (documento aparte), implementado en código recién el 26 jul.
- Descubierto que el repo ya tenía POC avanzado (Expo + Supabase) antes de esta ronda de sesiones.

---

## 10. Persistencia (cómo se guarda esto)

Este archivo vive en la carpeta local `~/Documents/donAR`. Claude lo lee al arrancar y lo actualiza al cerrar cada sesión, sin intervención de Gastón.

## 11. Pendientes / dudas para Gastón

- OK final sobre la lista de causas vetadas (sección 5).
- Definir si vale la pena construir edición real en el reenvío tras "pedido de info", o si el flujo actual alcanza por ahora.
- Decidir cuándo verificar un dominio propio en Resend (necesario para mandar mails a donantes reales, no solo a Gastón).
