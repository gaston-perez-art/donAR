import { Placeholder } from '@/components/placeholder';

export default function RankingScreen() {
  return (
    <Placeholder
      emoji="🏆"
      title="Ranking"
      subtitle="El top mensual de solidarios llega en la próxima rebanada."
    />
  );
}
