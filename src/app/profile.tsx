import { Placeholder } from '@/components/placeholder';

export default function ProfileScreen() {
  return (
    <Placeholder
      emoji="👤"
      title="Perfil"
      subtitle="Tu perfil único: lo que donás, lo que recibís, medallas y nivel."
    />
  );
}
