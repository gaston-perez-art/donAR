import { Placeholder } from '@/components/placeholder';

export default function CreateScreen() {
  return (
    <Placeholder
      emoji="➕"
      title="Crear una causa"
      subtitle="El flujo de crear y verificar tu causa llega en la próxima rebanada."
    />
  );
}
